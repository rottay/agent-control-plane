# Decisiones: mandatos, adjudicaciones y elecciones abiertas

Dueño único del concepto **decisión**: qué está resuelto, quién lo resolvió, qué
quedó explícitamente superseded, y qué sigue abierto sin bloquear el trabajo.

[Índice](../README.md) · [Arquitectura](../architecture/index.md) · [Datos](../architecture/database/index.md) · [Roadmap](../roadmap/index.md) · [Implementación](../implementation/index.md) · [Hallazgos](../findings/index.md)

Una decisión de esta página **no** es permiso para implementar, gastar cuota,
instalar nada ni publicar. Los permisos viven en
[implementación](../implementation/index.md) y los emite el owner.

---

## 1. Mandatos del owner, vigentes

| Tema | Decisión |
| --- | --- |
| Funcionalidad y neutralidad | Cumplir funciones reales con proveedores y frameworks elegibles mediante adapters; no acoplar el negocio a una marca |
| Prioridad de producto | Entregar casos de uso completos con arquitectura reemplazable; no integrar todas las herramientas ni construir adapters sin consumidor. Extensibilidad no equivale a soporte universal. [Priorización por entregables](../roadmap/index.md#11-casos-de-uso-antes-que-catálogo-de-integraciones) |
| Roles y modelos | Responsabilidades y modelos configurables y evolutivos; ninguna marca queda cableada como dueña permanente de un rol |
| Suscripciones, API y local | Mantener suscripciones, permitir clave de API y modelos locales bajo contratos reales. Que exista un login no implica soporte operativo |
| Cuentas | Identidad, cuota y renovaciones observables; continuidad al cambiar de cuenta con contexto durable; no automatizar el bypass de políticas del proveedor |
| Eficiencia | Eventos, checkpoints y contexto compacto; evitar polling intensivo y reauditorías circulares. Medir uso y latencia, no contar agentes |
| Gestión de esta implementación | **Orden del owner, 2026-09-22.** Claude en la cuenta claude-admin (`claude/opus/coordinator/01`) es DT y único coordinador: planifica, delega, adjudica y gestiona staging, receipts, commits locales y checkpoints; no es writer habitual ni aprueba cambios propios. Un solo Opus writer canónico en claude-admin (`claude/opus/implementer/01`, puede ser un subagente o sesión de terminal del DT); otros Opus read-only; sin fallback a `claude-daniel` ni a otra cuenta. Toda entrega, incluidas las ediciones propias del DT, pasa por verificación independiente antes del commit. Fable (`claude/fable/reviewer/01`) audita read-only a criterio del DT; si no está disponible, el cierre queda pendiente. Codex es consultor ocasional vía owner. Supera la asignación anterior (ver §3). Protocolo en [coordinación §0](../kickoff.md); no confundir esta flota con roles configurables del producto |
| Planificación previa | Preservar lo hecho. La auditoría contrasta el cierre; no trasplanta hallazgos viejos sin verificación |
| Anatomía | Folder/index, tests espejo separados, agrupaciones semánticas, tipos por dueño y sin duplicación de conceptos |
| **Separación de tipos** | Interfaces, enums y tipos **separados** del código de implementación, dentro del concepto y su dueño, sin depósito global ([arquitectura §7](../architecture/index.md)) |
| **Normalización de datos** | Nombres de tablas y columnas coherentes, diccionario completo e invariantes exactos ([datos](../architecture/database/index.md)) |
| UI | No se implementa UI en esta ronda. Primero el diseño; el backend contempla contratos sin construir visuales ni shell de escritorio |
| Producto local | No requiere plataforma multi-tenant ni bases compartidas de Rottay. Un login futuro va desacoplado de la autorización |
| Git | Sin ramas ni worktrees nuevos; conservar `main` y su estado. No se piden commits ni publicación para este trabajo documental |
| Cutover | P9 y la adopción en repositorios de producto no autorizados. Tener un plan nuevo no habilita adoptar el control plane allí |
| Herramienta de evaluación pesada | Se conserva la decisión de no instalarla en el grafo actual; productor y runner externos con alcance explícito |
| Consolidación documental | Autorizado consolidar `docs/audit` y retirar los borradores duplicados **después** de la verificación independiente. La retirada la ejecuta el root, no este writer |

---

## 2. Adjudicaciones técnicas de esta ronda

Resueltas con evidencia, sin consultar al owner, porque son elecciones de contrato,
de test o de atomicidad. Cada una tiene su desarrollo en el documento dueño.

| # | Decisión | Dónde vive |
| --- | --- | --- |
| 1 | El dominio **no** importa el paquete de persistencia: consume su propio puerto y el entrypoint adapta | [arquitectura §2](../architecture/index.md) |
| 2 | Un puerto se declara donde está su consumidor; un contrato del kernel que ya sirve se **reutiliza**, no se duplica; una interfaz que sólo usa un edge se queda en ese edge | [arquitectura §4](../architecture/index.md) |
| 3 | El vocabulario de un vendor va a su edge, incluidos pins y puertos; el kernel recibe sólo topología neutral | [estructura §4.2](../architecture/structure/index.md) |
| 4 | Las declaraciones de tipo viven en `types/`; `vocabulary/` sólo tiene valores; la derivación cruza en un solo sentido | [arquitectura §7](../architecture/index.md) |
| 5 | Grafía única `usecases/{mutations,queries}`, sin guion | [arquitectura §5](../architecture/index.md) |
| 6 | Cuatro streams; la configuración global va al stream de registry, **sin** iniciativa sintética | [datos §4](../architecture/database/index.md) |
| 7 | Escalera de identidad: tarea, revisión, intento, segmento, efecto lógico, intento de despacho, ocurrencia | [datos §6](../architecture/database/index.md) |
| 8 | La preimagen del envelope cubre **todos** los campos del contrato; el reloj y la cuenta quedan fuera | [datos §6.2](../architecture/database/index.md) |
| 9 | No existe hecho de costo independiente: el costo es un snapshot derivado con revisión de valuación | [datos §13](../architecture/database/index.md) |
| 10 | La identidad de bytes y la de acceso son distintas; compartir digest no concede permiso | [datos §12](../architecture/database/index.md) |
| 11 | La integridad del stream de cuentas se agrega por sidecar con baseline explícito, **sin** reescribir migraciones | [datos §9](../architecture/database/index.md) |
| 12 | La garantía de durabilidad se declara por perfil; SIGKILL no prueba pérdida de energía | [datos §10](../architecture/database/index.md) |
| 13 | `NO_COMMIT` permite editar dentro del write-set; `READ_ONLY` permite estado interno del plane | [contratos §12](../architecture/contracts/index.md) |
| 14 | Un efecto de desenlace desconocido no se reintenta: se reconcilia | [contratos §7](../architecture/contracts/index.md) |
| 15 | La habilitación operativa del commit exige el mínimo de recuperación | [contratos §12](../architecture/contracts/index.md) |
| 16 | `UNKNOWN` no es `0` en la rúbrica, y las compuertas críticas no se compensan con promedios | [calidad §1](../quality/index.md) |
| 17 | Se retiran como criterios: la relación numérica carpetas/archivos, la lista negra universal de nombres y el `grep` de marca como prueba semántica | [calidad §2](../quality/index.md) |
| 18 | Los objetivos numéricos de release quedan **fijados ahora** como objetivos de diseño, con perfil y carga; el implementador no los inventa ni los ajusta después del resultado | [tests §9](../quality/testing/index.md) |
| 21 | Contrato de contenido v1 y contrato de resultado v1 congelados; error de herramienta produce resultado fallido aunque el transporte responda bien | [contratos §4](../architecture/contracts/index.md) |
| 22 | `COMPLETED` es el terminal exitoso; `NO_COMMIT` termina en `COMPLETED` sin fabricar un commit | [contratos §2.1](../architecture/contracts/index.md) |
| 23 | La admisión ejecutable **rechaza** verificador o auditor igual al writer por identidad emitida por el supervisor; el mismo proveedor sólo produce una recomendación | [contratos §5](../architecture/contracts/index.md) |
| 24 | La idempotencia de sumisión es por clave del cliente; el digest del envelope es una precondición comparada, no parte de la clave | [contratos §15](../architecture/contracts/index.md) |
| 25 | El ledger y un coordination store no comparten transacción: lo atómico es la intención, y el acuse se reconcilia por saga | [datos §11](../architecture/database/index.md) |
| 26 | Un solo registry de modelos, propiedad de `accounts`; `economy` lo referencia por identificador y digest | [datos §4](../architecture/database/index.md) |
| 19 | La suite completa no se corre hasta aislar los tests que escriben el checkout vivo | [tests §5](../quality/testing/index.md) |
| 20 | Una familia se anuncia **intercambiable** sólo con dos implementaciones reales conformes; **funcional** admite una | [integraciones §2](../architecture/integrations/index.md) |
| 27 | Aceptar la revisión K3 con correcciones, preservando arquitectura y alcance; el root adjudica, los reviewers no gobiernan ni editan el canon | [revisión independiente](../evidence/review/index.md) |
| 28 | Compatibilidad por operación, responsabilidad, recurso y scope; selección explícita, snapshot inmutable y evidencia específica antes de binding/dispatch | [composición](../architecture/integrations/composition/index.md) |
| 29 | Preservar vocabularios originales de capacidades y estados legacy; evaluación neutral e historia etiquetada, sin coerciones a éxito/soporte | [contratos §2.0–2.2](../architecture/contracts/index.md) |
| 30 | El ledger admite la cuota incluyendo débitos pendientes; arbiter concede slot. Liberarlo no borra gasto sin observación reconciliada | [cuentas §4](../architecture/database/accounts/index.md) |
| 31 | Outbox anclada a stream/secuencia/hash de un sujeto real; CAS por row_version, estado y encarnación. No hay tarea SYSTEM ni segundo outbox | [coordinación §6](../architecture/database/coordination/index.md) |
| 32 | Un requisito tiene un packet primario y un hito de aceptación; entregas internas no aumentan porcentajes ni sustituyen gates | [correspondencia](../implementation/packets/requirements/index.md) |
| 33 | Cortes de identidad, artefactos, catálogo, captura y admisión preceden a sus consumidores; desarrollo no equivale a habilitación | [paralelismo](../roadmap/parallelism/index.md) |
| 34 | Un invocador CLI opaco puede ser funcional en su frontera probada; no se le adjudican control de retries internos, hard cap o telemetría inexistentes | [composición §7](../architecture/integrations/composition/index.md) |
| 35 | El puente documental utiliza ADR sucesor y conserva el cuerpo histórico congelado; no cambia la ley durante la auditoría para autoaprobarse | [migración](../implementation/migration/index.md) |
| 36 | Estimaciones y desempeño fijan política, cohorte y vector de fuentes; UNKNOWN se propaga por dependencias y recursos. Simular no consume cuota ni ejecuta modelos | [estimación](../architecture/contracts/estimation/index.md) |
| 37 | Avisos locales reutilizan outbox y sujeto reales; recepción durable no significa lectura humana. Rate y reloj sobreviven a replay y a cambios de policy | [interacción §2](../architecture/contracts/interaction/index.md) |
| 38 | Espera humana con plazo/CAS y cota temporal durable; grant sólo habilita revalidación, aviso/timer no conceden permiso | [interacción §3](../architecture/contracts/interaction/index.md) |
| 39 | Duelo QUALITY_ONLY compara dos tareas READ_ONLY/NO_COMMIT con árbitro independiente; costo UNKNOWN visible no impide ganador técnico ni acredita ventaja económica | [interacción §4](../architecture/contracts/interaction/index.md) |
| 40 | Anomalías reutilizan la estadística de estimación; detección, intención y confirmación son distintas. No repausar ni ampliar autoridad por detecciones repetidas | [interacción §5](../architecture/contracts/interaction/index.md) |
| 41 | `task_revision_read_model` nace sin `envelope_artifact_reference_id`: la columna la agrega P-36/local por `ADD COLUMN` + trigger `BEFORE INSERT` por cohorte de `contract_version` + clave en el payload del evento de revisión; `NULL` en toda revisión anterior; **nunca** se inventa una referencia por digest | [execution §2](../architecture/database/execution/index.md); ADR de P-05/B |
| 43 | El tope de 10.000 intentos acumulados por tarea se mantiene y el CAS lo conoce: el desborde es un rechazo tipado documentado, nunca un overflow opaco; una tarea que agota la cota se resuelve con una tarea nueva | [ejecución §3](../architecture/database/execution/index.md); ADR 0073 |
| 42 | El namespace `v2/` y la composición de la clave V2 viven en `@acp/contracts`, no en `@acp/ledger` — revisa el emplazamiento que declaró P-05/B («el separador se declara acá y en ningún otro lado»): un namespace es gramática de la clave, y la clave es del contrato. El ledger lo importa; el literal queda en un solo archivo `src`. La puerta es **estricta**: coordenada V2 completa en el payload ⇔ clave V2; sin coordenada ⇔ clave V1, y ninguna otra forma pasa — un productor no cambia de namespace para convertir un conflicto en un hecho nuevo | [streams §1.1](../architecture/database/streams/index.md); [ADR 0072](../../architecture/0072-the-v2-key-composes-at-the-contract.md) |
| 44 | El outbox **no deriva** `command_id`: la clave es determinista por `(saga_id, phase, target_kind, target_id)` y la computa el productor en P-18/F, mientras el store impone `UNIQUE` y nada más — un store que la derivara tendría gramática de saga, y la gramática de una clave es del contrato (mismo reparto que la decisión 42). Si resulta ser gramática, F la emplaza en `@acp/contracts`; E2 no lo prejuzga y un test fija que el store no deriva la clave | [coordinación §6](../architecture/database/coordination/index.md); [ADR 0074](../../architecture/0074-the-outbox-is-a-store-with-a-version.md) |
| 45 | `last_failure_code` es `TEXT` **sin CHECK**: el vocabulario tipado vive en [contratos §16](../architecture/contracts/index.md) y lo impone el escritor (P-18/F), no el esquema. Un CHECK acoplaría una migración inmutable de `outbox.sqlite` a un catálogo que no lo es, y cada crecimiento del catálogo sería una migración de esta base. El costo queda declarado: hasta que F escriba, nada impide a un escritor directo guardar un código que §16 no define — una sola columna, nombrada, con dueño | [coordinación §6](../architecture/database/coordination/index.md); [ADR 0074](../../architecture/0074-the-outbox-is-a-store-with-a-version.md) |
| 46 | El retrofit de `worktree_lease` que pide [coordinación §3](../architecture/database/coordination/index.md) `:90-107` —validadores `BEFORE INSERT`/`BEFORE UPDATE` y CAS por token esperado con los cuatro casos de la regla de fence— es **packet propio**, con destino declarado. E1 hace sólo lo aditivo: `coordination_store_meta`, `store_incarnation_id`, y `operation_id`/`revocation_acknowledged_at` como columnas que ningún verbo del store escribe. La aritmética del fence no cambia: un retrofit que moviera la semántica del fence tocaría a C2/C3/C4, que este escalón no puede abrir | [coordinación §3](../architecture/database/coordination/index.md); [ADR 0075](../../architecture/0075-every-coordination-store-names-its-own-incarnation.md) |
| 47 | La autoridad de identidad de un archivo de coordinación es su `store_kind`, **no el nombre del archivo**: [coordinación §1](../architecture/database/coordination/index.md) dice `worktree-leases.sqlite` y el árbol dice `leases.sqlite`, y ninguno se renombra. Renombrar el archivo de un árbitro vivo cuesta vivacidad y no compra nada; lo que se comprueba en `open` es el kind declarado, contra el diccionario de cinco de §8.1. Por eso el CHECK carga los cinco kinds y no sólo el propio: estrechado a uno, el rechazo del kind ajeno sería inconstruible, y una guardia que nadie puede ejercitar no es una guardia | [coordinación §8.1](../architecture/database/coordination/index.md); [ADR 0075](../../architecture/0075-every-coordination-store-names-its-own-incarnation.md) |
| 48 | `CONTRACT_VERSION` pasa a `"2.3.0"` en P-18/protocolo C, y la regla de admisión es **«se emite sólo la vigente»**: `SUPPORTED_CONTRACT_VERSIONS` (lectura) queda en `["2.2.0", "2.3.0"]` y `"2.2.0"` se queda para siempre, mientras `AdmittedContractVersion` (emisión) pina la vigente en los tres shapes que el ADR 0072 nombró —`TaskEnvelope`, `WorkerSlot`, `CommitAuthorizationReceipt`— y en la puerta de append del ledger para una inserción **nueva**. La exención es el replay exacto: un evento ya registrado, reenviado idéntico, devuelve su registro sin llegar al chequeo. `ControlPlaneEvent` conserva el set de lectura a propósito, porque es el schema con el que el ledger re-parsea cada fila almacenada. El criterio por el cual C porta el bump y B no: los payloads de C llevan digests que el fold verifica y una versión de contrato propia por payload, es decir historia cuya reconstrucción depende de preimágenes nuevas. Consecuencia declarada (V3): `envelope_sha256` difiere para el mismo trabajo emitido antes y después, y los vectores pineados de `envelope-identity` se mueven con el fixture | [streams §1.1](../architecture/database/streams/index.md); [ADR 0072](../../architecture/0072-the-v2-key-composes-at-the-contract.md); [ADR 0076](../../architecture/0076-an-effect-is-looked-up-by-its-logical-key.md) |
| 49 | Las ocurrencias de [ejecución §8](../architecture/database/execution/index.md) llegan en P-18/protocolo D con **dos** tipos de evento, `PROMPT_OCCURRENCE_RECORDED` y `RESPONSE_OCCURRENCE_RECORDED`, porque la respuesta tiene PK, instante y secuencia propios y puede llegar tardía. `prompt_sha256` no es único y `dispatch_attempt_id` es FK no única: los mismos bytes enviados dos veces son dos ocurrencias y un solo digest. El `ordinal` lo asigna el ledger (uno más que el mayor del segmento, `0` si no hay), `identity` es el `emittedBy` del evento, el cuarteto de modelo viaja en el payload del prompt y `REDACTION_VERDICTS` vive en `@acp/ledger` (clase de la decisión 45). Los tres digests se **conservan sin recomputar**: sus preimágenes son los bytes que §8 `:433` deja fuera. Los payloads son cerrados, y la respuesta no puede nombrar despacho, segmento ni cuenta: se atribuye sólo por su prompt, y su coordenada V2 es la del prompt. `prompt_record_read_model` nunca existió en este árbol. **Sin bump**: D agrega hechos (digests conservados, conteos, vocabulario) y ninguna fórmula de identidad, que es el criterio del ADR 0076 para portar el bump; `CONTRACT_VERSION` sigue `"2.3.0"` | [ejecución §8](../architecture/database/execution/index.md); [streams §1.1](../architecture/database/streams/index.md); [ADR 0076](../../architecture/0076-an-effect-is-looked-up-by-its-logical-key.md); [ADR 0077](../../architecture/0077-a-prompt-occurrence-is-a-use-never-a-blob.md) |
| 50 | `CONTRACT_VERSION` pasa a `"2.4.0"` en P-18/protocolo F y `SUPPORTED_CONTRACT_VERSIONS` queda en `["2.2.0", "2.3.0", "2.4.0"]`, con la misma regla de la decisión 48: se emite sólo la vigente, el replay exacto está exento y `"2.3.0"` se lee para siempre. El criterio del ADR 0076 da a favor por sus dos brazos: la puerta **y** el fold recomputan `command_id` sobre una preimagen nueva, y cada payload de outbox lleva `outboxContractVersion`. La preimagen se fija como `OUTBOX_COMMAND_ID_PREIMAGE_PREFIX_V1 + canonicalJson([sagaId, phase, targetKind, targetId])` con el prefijo en `@acp/contracts` y el cómputo en `@acp/ledger` —el lugar que la decisión 44 anticipó «si resulta gramática»—. La causalidad viaja sólo en `CausationRef`, nunca como digest en el payload. Consecuencia V3 declarada otra vez: los vectores de `envelope_sha256` se mueven con el fixture | [coordinación §6](../architecture/database/coordination/index.md); [datos §11](../architecture/database/index.md); [ADR 0076](../../architecture/0076-an-effect-is-looked-up-by-its-logical-key.md); [ADR 0078](../../architecture/0078-a-command-intention-commits-with-its-quarantine.md) |
| 51 | Dentro del ledger la cuarentena y la intención `REVOKE_LEASE` son atómicas: la intención se admite sólo inmediatamente después de un evento de cuarentena (`WRITE_SET_VIOLATION_DETECTED` o `TASK_STATE_CHANGED → SUSPECT_WORKTREE`) de la misma tarea **insertado por la misma transacción**, y dentro de `appendBatch` un lote que mueve una tarea a `SUSPECT_WORKTREE` sin su intención se revierte entero. El `append` unitario de ese movimiento queda como **ventana legacy declarada** en un solo sitio, el gate de conformance del daemon (`daemon/src/composition/ports`), que hoy registra la violación en tres transacciones; `L-P18F-1` lo nombra y falla ante un segundo sitio, y la cierra la adopción, no este packet. `LEASE_REVOKED` no integra el lote: dentro de la transacción del ledger nada ocurrió todavía en el arbiter | [contratos §13](../architecture/contracts/index.md); [datos §11](../architecture/database/index.md); [ADR 0078](../../architecture/0078-a-command-intention-commits-with-its-quarantine.md) |
| 52 | `worktree_lease` gana los dos verbos de [coordinación §3](../architecture/database/coordination/index.md) `:96-97`: `REVOKE { at, operationId }` exige lease vivo, asigna `fence = OLD.fence + 1`, despeja la titularidad y estampa `operation_id`; `ACKNOWLEDGE_REVOCATION { at }` exige una fila revocada sin acuse y **conserva** el fence. `LeaseGrant.operationId` es opcional y lo estampa `GRANT`, que además despeja un acuse previo; `RELEASE` y `sweep` de un grant **vivo** despejan `operation_id`, para que una fila liberada nunca pase por revocada. La aritmética del fence gana ese caso; los validadores y el CAS por token de la decisión 46 siguen siendo packet propio | [coordinación §3](../architecture/database/coordination/index.md); [ADR 0075](../../architecture/0075-every-coordination-store-names-its-own-incarnation.md); [ADR 0078](../../architecture/0078-a-command-intention-commits-with-its-quarantine.md) |
| 53 | El vocabulario de `last_failure_code` es `OUTBOX_FAILURE_CODES`, cerrado en seis palabras (`TARGET_REFUSED`, `TARGET_STALE_TOKEN`, `TARGET_INCARNATION_MISMATCH`, `TARGET_UNAVAILABLE`, `DEADLINE_EXCEEDED`, `NOT_DISPATCHED_PROVEN`), declarado en `@acp/contracts` junto a los tres tipos e impuesto **al escribir** por la puerta de `OUTBOX_DELIVERY_OBSERVED`: obligatorio con `FAILED_RETRYABLE` y `FAILED_TERMINAL`, admitido con `ABANDONED`, rechazado con cualquier otro estado. Es el `code` del registro de [contratos §16](../architecture/contracts/index.md) para `origin ∈ {DURABILITY, EXECUTION}` y `phase ∈ {DISPATCH, RECOVERY}`; el mapa exhaustivo de §16 es packet posterior y la columna de E2 sigue guardando `TEXT` (decisión 45) | [contratos §16](../architecture/contracts/index.md); [ADR 0074](../../architecture/0074-the-outbox-is-a-store-with-a-version.md); [ADR 0078](../../architecture/0078-a-command-intention-commits-with-its-quarantine.md) |
| 54 | F **registra y pliega**; no construye reconciliador ni dispatcher, que son de P-18/recuperación. Quedan como reglas vinculantes para ellos: (O-2) un `INFLIGHT` vencido **sin ancla** sale a `FAILED_RETRYABLE`, no a `RECONCILING`, porque sin intento durable no hubo despacho; (O-3) un rechazo de trigger del outbox es excepción `LedgerQueryError`, no un cuarto verbo, y nunca se lee como éxito ni como motivo de reenvío; y una cache reconstruida escribe un intento sin desenlace como `RECONCILING`, conservando ancla y encarnación del destino. El fold de F pliega un intento sin observación a `RECONCILING`, nunca `PENDING` ni `INFLIGHT`, y las fronteras de crash 1-3 se prueban en proceso; la matriz SIGKILL es del perfil certificado | [coordinación §2 y §6](../architecture/database/coordination/index.md); [testing §7 y §10](../quality/testing/index.md); [ADR 0078](../../architecture/0078-a-command-intention-commits-with-its-quarantine.md) |
| 55 | **Un desenlace conocido se reutiliza, nunca se reentrega** (CORR-2): la puerta de despacho refusa `DISPATCH_INTENDED` sobre un efecto cuyo `outcome_status` no es nulo, los cuatro valores. `OUTCOME_UNKNOWN` conserva su rechazo y su texto; `SUCCEEDED`, `FAILED` y `CANCELLED` refusan con el mensaje terminal en `payload.dispatch.effectId`, **sin excepción** para `FAILED` ni `CANCELLED` —reintentar uno sería decisión escrita del owner, no default—. La guardia lee el desenlace del efecto y no el estado de las entregas: tras una entrega `ABANDONED`, o `SETTLED` sin desenlace, la siguiente se admite, y el replay exacto de la primera sigue siendo replay. Sólo puerta, con el precedente de O-1: el fold no cambia; sin contrato, migración ni versión | [ejecución §6.1](../architecture/database/execution/index.md); [ADR 0076](../../architecture/0076-an-effect-is-looked-up-by-its-logical-key.md); [ADR 0079](../../architecture/0079-a-known-outcome-is-reused-and-a-present-invalid-word-is-refused-by-name.md) |
| 56 | **Presente-inválido no es ausente** (CORR-2): `dispatchOutcomeRecord` devuelve la unión rechazante `DispatchOutcomeReading` (forma de `OccurrenceReading`, escalón D) y cada uno de los cuatro campos opcionales de una resolución —`effectOutcomeStatus`, `acceptedAt`, `externalHandle`, `providerIdempotencyKey`— se lee de tres maneras: clave ausente → `null`; valor legal (palabra de `EFFECT_OUTCOME_STATUSES` o texto no vacío) → el valor; cualquier otro valor presente —palabra fuera del vocabulario, número, objeto, cadena vacía o `null` JSON **explícito**— → rechazo en `payload.outcome.<campo>`, con el valor mostrado sólo vía `printable` y nunca como bytes arbitrarios. La puerta y `applyEventToSnapshot` lanzan el mismo issue, así que un historial plantado falla el rebuild con las palabras de la puerta. Un payload que no constituye resolución sigue leyéndose `null`. Los tipos de valor de la era P-18 pasan a hojas de tipos puras del concepto dueño (`src/projection/types`, `src/outbox-store/types`), re-exportadas sin cambio de imports | [ejecución §6 y §7](../architecture/database/execution/index.md); [ADR 0077](../../architecture/0077-a-prompt-occurrence-is-a-use-never-a-blob.md); [ADR 0079](../../architecture/0079-a-known-outcome-is-reused-and-a-present-invalid-word-is-refused-by-name.md) |
| 57 | **El productor habla la coordenada V2** (P-18/protocolo G): `DurableInvocation` gana `revision` **opcional** (`revisionId`, `revisionNumber`, `attemptNumber`, `envelopeSha256`), llevada por `deriveInvocation` sin entrar en la preimagen de `invocationId`; sin ella, cada clave, id y payload es byte-idéntico a `a6ed7c3` (dos vectores fijados). Con ella, cada evento lleva la coordenada y su clave se compone por `buildV2IdempotencyKey` importada (N-P18-20). **La apertura es el primer evento de la tarea** (Q-G1, opción (c), enmienda la posición de H-5): `TASK_ATTEMPT_OPENED` fuera de plan (`attempt.opened`, `null → DISCOVERED`, instantes de la sumisión, sin causación, payload de B campo por campo) y el descubrimiento la sigue en `DISCOVERED → DISCOVERED`, porque la aritmética del ledger (`1 + MAX(attempt)` por tarea, repetido por toda la coordenada) no admite otra posición. El productor propone `1 + latestAttempt` y refusa antes del append si difiere de la invocación; ningún paso V2 antes de su apertura (O-2 de B estrechado **en el productor**). Sin bump: G no agrega tipo, preimagen ni fórmula (criterio de 0076). Los productores de efecto, despacho y ocurrencia se **reasignan** a adopción/recuperación; los productores excepcionales y `restateInvocation` fallan cerrado ante una tarea V2 | [ejecución §3 y §6.1](../architecture/database/execution/index.md); [streams §1.1](../architecture/database/streams/index.md); [ADR 0073](../../architecture/0073-every-attempt-opens-with-its-own-identity.md); [ADR 0080](../../architecture/0080-the-producer-speaks-the-v2-coordinate.md) |
| 58 | **Un artefacto es sujeto del registry antes de que se mueva su primer byte** (P-36/local A): la migración 15 **reconstruye** `registry_events` —la única vía, porque un CHECK no se ensancha en sitio— en el orden E3 que la preauditoría verificó: tabla nueva, copia ordenada con `sequence`, drop de los tres triggers propios **y de los dos ajenos** (`tr_control_plane_events__validate_new_rows`, `tr_initiative_events__validate_new_rows`, que nombran la tabla y abortan el `RENAME`), drop, rename, índices y triggers recreados con los mismos nombres —los ajenos byte-idénticos a la 9— y recién entonces los cuatro read models. Sin pragma. Ninguna fila cambia: la cadena digiere `previous_sha256` y `event_json`, copiados. `subject_kind IN ('DOCUMENT','ARTIFACT')` sin default, `document_kind` NULL sólo en ARTIFACT, `artifact_event_kind` con CHECK sobre los **nueve** nombres y espejos como igualdades de verdad. Fila ARTIFACT (H-3): `document_id` = sujeto (`content_sha256` / `artifact_reference_id` / `artifact_pin_id`), `document_version` = ordinal `1 + MAX` propuesto por el productor y verificado por el ledger, `content_digest` = contenido, `effective_from` = `occurredAt`; `subjectKind` y `artifactEventKind` también en el cuerpo. **Tras la 15, cuatro tablas referencian `registry_events` por FK: una reconstrucción futura tira primero a los hijos** | [artefactos §1.1 y §3-6](../architecture/database/artifacts/index.md); [streams §4](../architecture/database/streams/index.md); [ADR 0081](../../architecture/0081-an-artifact-is-a-subject-of-the-registry.md) |
| 59 | **La política de acceso es un identificador cerrado en código, sin FK ni CHECK** (adjudicación 3): `access_policy_id` admite sólo `SCOPE_EQUALITY_V1` (`ARTIFACT_ACCESS_POLICY_IDS`, en `@acp/ledger`) y la puerta rechaza cualquier otro por nombre. `access_policy_read_model` no tiene diccionario y no se inventa; la FK de artefactos §4 queda sin efecto bajo una línea de errata en la hoja, hasta que el dueño de la política la diccione. Un CHECK convertiría una segunda política en una reconstrucción del read model; la lista cerrada la vuelve una edición. La misma errata registra que §8.1 lista nueve eventos y A entrega seis: `RECLAIM_INTENDED`, `RECLAIM_COMPLETED` y `REFERENCE_TOMBSTONED` se rechazan por nombre en `artifactEventKind` (N-P36-18) | [artefactos §4 y §8.1](../architecture/database/artifacts/index.md); [ADR 0081](../../architecture/0081-an-artifact-is-a-subject-of-the-registry.md) |
| 60 | **El resolver de credenciales no existe** (adjudicación 4): la cláusula «resolver de credenciales existente» de `P-36/local` en el registro de paquetes se corrige. En A es un **negativo**: un centinela de credencial en un evento de artefacto lo rechazan las guardas del contrato (`ArtifactRegistryEvent` corre `findCredentialViolations` y `findTranscriptViolations` sobre todo el evento) y no llega ni al error ni al stream, y `SECRET_BEARING` se rechaza por nombre porque nunca se publica en el stream. El `CredentialResolverPort` real es de P-19; ningún escalón de P-36/local persiste ni resuelve un secreto | [artefactos §10](../architecture/database/artifacts/index.md); [paquetes](../implementation/packets/index.md); [ADR 0081](../../architecture/0081-an-artifact-is-a-subject-of-the-registry.md) |
| 61 | **Lecturas del fold de artefactos que §8.1 no deletrea** (P-36/local A): la generación la **propone** el productor y la verifica el fold —`1 + highest` si no hay generación no reclamada; la no reclamada si la hay—. Una intención sobre `PUBLISHED` deduplica y conserva la fila entera; sobre `PUBLICATION_ABANDONED` la reescenifica con su `grace_started_at`; sobre `STAGED` se **rechaza**, porque hay una publicación en vuelo. Una generación reutilizada conserva estado de cifrado, referencia de clave y perfil: el desacuerdo es `LedgerArtifactEncryptionConflictError`, el error nombrado de §3/§10.1, sin valores en el mensaje; un `sizeBytes` distinto se rechaza. El pin `PUBLICATION` lo toma la intención con `pinHolderId = commandId` y lo liberan sólo su éxito o su abandono, que exigen ese pin vivo; `PIN_ACQUIRED`/`PIN_RELEASED` no lo tocan. `REFERENCE_RECORDED` exige `PUBLISHED`. Retomar el mismo pin es idempotente; un segundo id vivo del mismo holder y el id de un pin liberado se rechazan. Todas las refusas viven en una sola función de decisión que usan la puerta y el rebuild | [artefactos §3, §5 y §8](../architecture/database/artifacts/index.md); [ADR 0081](../../architecture/0081-an-artifact-is-a-subject-of-the-registry.md) |
| 62 | **El archivo del lease de blobs se llama `artifact-blob-leases.sqlite` y tiene un solo productor** (P-36/local B, Q-P36B-2): ninguna autoridad lo nombra —[coordinación §1](../architecture/database/coordination/index.md) listaba cuatro archivos y [artefactos §7](../architecture/database/artifacts/index.md) dice «archivo separado»—, así que se decide aquí. Vive junto al ledger y lo compone sólo `artifactBlobLeaseStorePath(ledgerPath)`, patrón `outboxStorePath`: el publicador y el reconciliador de C son dos puertas y un recolector será la tercera, y tres puertas que componen la ruta son tres árbitros. La ley `L-P36B-3` lo custodia y la hoja de coordinación §1 gana la fila del quinto archivo. Como en la decisión 47, la identidad es su `store_kind` `ARTIFACT_BLOB_LEASE`, no el nombre | [coordinación §1](../architecture/database/coordination/index.md); [ADR 0082](../../architecture/0082-a-blob-lease-excludes-the-second-publisher.md) |
| 63 | **Lecturas del lease de blobs que §7 no deletrea** (P-36/local B): el store admite `PUBLISH` y `RECLAIM` como dato, con el mismo mecanismo (Q-P36B-1), y ninguna palabra por nombre. Cuatro verbos, cada uno una transacción `IMMEDIATE`: `acquire` (inserta en 1 o avanza una fila libre; `HELD` sobre cualquier tenencia, vencida o no; replay de la misma `operation`, `operation_id` y holder → `UNCHANGED`), `release` (token completo: encarnación contra la meta viva y contra la fila, luego generación, tenencia e identidad; conserva la generación), `revoke` y `takeOver` (terminan la tenencia de otro con `OLD + 1`). Estos dos exigen en la firma una atestación de quiescencia `DEATH_AND_REAP_PROVEN` o `STALE_FENCE_REFUSED_BY_BACKEND` con el pid al que se refiere: el store rechaza la que nombra otro proceso y no verifica nada más — la prueba es del llamador (§8 `:266-267`, §9 `:343-347`). Las refusas son valores de un conjunto cerrado de hechos del archivo; no hay clase de error nueva, ni `sweep`, ni verbo de renovación. Una tenencia escrita bajo una encarnación rotada queda **congelada**, no liberada, y un archivo con filas y sin meta se rechaza al abrir en vez de adoptarse; el procedimiento que reemite tenencias es de §8.2. El store no lee el ledger (H-1). El seam de fallos para C es el borde de cada verbo más `__testFaults.beforeLeaseCommit` | [artefactos §7-§9](../architecture/database/artifacts/index.md); [coordinación §8](../architecture/database/coordination/index.md); [ADR 0082](../../architecture/0082-a-blob-lease-excludes-the-second-publisher.md) |
| 64 | **La intención de una publicación lleva la referencia que registrará** (P-36/local C, H-2 y C-1 adjudicadas): `PublicationIntendedPayload` gana `intendedReference`, el `ArtifactReferenceRecord` **entero** —con su `artifactReferenceId` y sus dos refinamientos heredados—, opcional en el esquema. Sin él nada durable dice qué referencia completa la reconciliación 3→4 con bytes válidos (§8 `:260-262`); con él, la referencia completada es **la original**, campo por campo, y `assertReferenceIsNew` la sigue verificando en el éxito. Son **hechos, no identidad**: el fold no lee el bloque, nada se deriva de él y un cuerpo sin él parsea y encadena como antes, así que `CONTRACT_VERSION` `2.4.0` y `SUPPORTED_CONTRACT_VERSIONS` quedan quietos (criterio de ADR 0076). La opcionalidad es del esquema, por la historia: el plano privado **siempre** lo escribe y `publish` exige la referencia. Una intención sin bloque —de otro productor— se abandona aunque los bytes verifiquen; los bytes quedan. El mismo cuerpo bajo la misma clave es replay; otra referencia bajo la misma clave es `LedgerIdempotencyConflictError`. El fold **no** compara la referencia del éxito con el bloque: esa igualdad la debe el plano | [artefactos §8-§10](../architecture/database/artifacts/index.md); [ADR 0076](../../architecture/0076-an-effect-is-looked-up-by-its-logical-key.md); [ADR 0083](../../architecture/0083-a-publication-names-its-bytes-only-after-they-survive-the-fsync.md) |
| 65 | **La subraíz del plano privado se llama `private-artifacts/` y tiene un solo productor** (P-36/local C, C-6; adjudicación 2 del mapa): ninguna autoridad la nombra, así que se decide aquí. Es hermana de `artifacts/` junto al ledger —no está debajo—, de modo que ningún lector del almacén legado por digest desnudo alcanza un objeto privado (N-P36-2). La compone sólo `artifactPlaneRootFor(ledgerPath)`, que **no** llama a `artifactRootFor`, cuya casa única custodia `L-F3-2`. Se crea `0700` sin `recursive`, se resuelve **una vez** al abrir y cada uso comprueba su dispositivo e inodo. El staging vive en el shard (`<digest>.staging`), mismo filesystem, derivado sólo del digest comprobado. La ley `L-P36C-4` custodia el productor único, precedente de la decisión 62 | [artefactos §8-§10](../architecture/database/artifacts/index.md); [ADR 0083](../../architecture/0083-a-publication-names-its-bytes-only-after-they-survive-the-fsync.md) |
| 66 | **Lecturas del plano privado que §8-§10 no deletrean** (P-36/local C): (a) **identidades del llamador** —ids y claves de la intención y del evento terminal, `commandId`, `artifactPinId`, `artifactReferenceId`, holder, pid y atestación—; el plano no lee reloj, proceso ni entorno y no acuña nada (H-7, H-8). (b) El plano computa `content_sha256` y `size_bytes`; un digest o tamaño declarado distinto, contenido sobre `ARTIFACT_PLANE_CONTENT_MAX_BYTES` (16 MiB, cota de memoria), `ENCRYPTED_AT_REST`, `SECRET_BEARING`, una política fuera de `SCOPE_EQUALITY_V1` y un centinela de credencial en cualquier metadato se rechazan **antes** del lease (N-P36C-4, N-P36C-16, N-P36-17). (c) Antes de cada mutación de filesystem y antes del éxito se relee el token: si la tenencia ya no está, `LEASE_SUPERSEDED` y nada más se escribe —defensa en profundidad; la garantía es la quiescencia (§9 `:343-347`, H-10)—. (d) Destino existente: verifica → ni escritura ni rename; no verifica o es enlace → nada se sobrescribe ni se borra y la publicación se abandona (N-P36C-2). (e) **Reconciliación** por digest: tenencia `PUBLISH` sin pin vivo de su comando (1→2, 4→5) → `revoke`; con pin vivo → `takeOver` con el mismo `operation_id` y pasos 3-5 sin bytes; sin tenencia y con pin vivo → `acquire`; sin atestación o sobre otro pid → nada se mueve (N-P36C-8); una tenencia `RECLAIM` bajo el mismo `operation_id` → `HELD_FOR_RECLAIM` también en `publish` (O-2 de la postauditoría C). **Ninguna refusa deja un digest tenido** (C-1 de la postauditoría C): un `artifactReferenceId` que el ledger ya registra se rechaza antes del lease —salvo el éxito ya registrado de este mismo comando, que un reintento replaya—; si la puerta rehúsa el `PUBLICATION_SUCCEEDED` en el paso 4 (referencia registrada entre tanto, bloque que ningún éxito puede llevar) con `LedgerValidationError`, el plano registra `PUBLICATION_ABANDONED` con la **misma** identidad terminal y responde `ABANDONED`/`REFERENCE_REFUSED_BY_DOOR` —los bytes quedan, la generación conserva `grace_started_at`, la siguiente intención la reescenifica (decisión 64)—; cualquier otro error de un append terminal suelta la tenencia propia y se relanza, con el pin vivo para un reintento con identidad corregida (N-P36C-19, N-P36C-20). (f) El publicador reiniciado **no** replaya `acquire`: vuelve a `publish` con atestación, toma la tenencia, **encuentra** su intención y sigue desde el paso 3; `UNCHANGED` queda para la reentrada en proceso (mismo holder, pid y encarnación), y quien desplaza debe llamarse distinto del desplazado, porque `takeOver` bajo el mismo holder es replay y dejaría el pid muerto en la fila (C-5; O-1 de la postauditoría B). (g) Lector: igualdad de `(scope_kind, scope_id)`, `SYSTEM` sólo para un lector `SYSTEM`, la misma palabra para ajena y ausente (N-P36C-9); vencer no se lee (N-P36-16). (h) Ocho seams de fallo entre pasos; la reconciliación se ejercita desde una instancia **nueva** del plano; sin `SIGKILL` ni worker. (i) El ledger expone verbos de sólo lectura sobre la vista del fold (H-1); la puerta y los folds no cambian | [artefactos §8-§10](../architecture/database/artifacts/index.md); [ADR 0082](../../architecture/0082-a-blob-lease-excludes-the-second-publisher.md); [ADR 0083](../../architecture/0083-a-publication-names-its-bytes-only-after-they-survive-the-fsync.md) |
| 67 | **Una revisión de la cohorte nueva nombra su envelope por referencia, nunca por digest** (P-36/local D, decisión 41): la migración 16 agrega `task_revision_read_model.envelope_artifact_reference_id` por `ADD COLUMN` —nullable, sin default, sin reconstruir la tabla ni sembrar watermark— y un trigger `BEFORE INSERT` de **dos lados**, `tr_task_revision_read_model__validate_envelope_reference`: `contract_version IN ('2.2.0', '2.3.0', '2.4.0')` con valor → `RAISE`; cualquier otra versión sin valor o con `''` → `RAISE`. La cohorte previa es una **lista cerrada congelada en la 16**, nunca una comparación de cadenas, y el fold la deletrea igual (`PRE_ENVELOPE_REFERENCE_CONTRACT_VERSIONS`). **El bump paga la cohorte, no la identidad** (H-2): `CONTRACT_VERSION` pasa a `"2.5.0"` y `SUPPORTED_CONTRACT_VERSIONS` a `["2.2.0", "2.3.0", "2.4.0", "2.5.0"]`, con la regla de la decisión 48. El criterio del ADR 0076 sigue para bumps por identidad y solo no lo portaría —la referencia es un **hecho**, como `intendedReference` (decisión 64)—; la decisión 41 agrega un segundo motivo legítimo: cuando una ley fija una cohorte por versión, la versión es el discriminador y debe moverse. Precisión de [ejecución §2](../architecture/database/execution/index.md) `:73`: donde dice «NOT NULL desde P-36/local», lo exacto es **exigida desde `2.5.0`**. Consecuencia V3 declarada otra vez: los vectores de `envelope_sha256` se mueven con el fixture | [ejecución §2](../architecture/database/execution/index.md); [ADR 0067](../../architecture/0067-the-revision-coordinate-rides-the-stream-it-already-has.md) §5; [ADR 0076](../../architecture/0076-an-effect-is-looked-up-by-its-logical-key.md); [ADR 0084](../../architecture/0084-a-revision-names-its-envelope-by-reference-never-by-digest.md) |
| 68 | **Forma en el fold, existencia en la puerta, y la referencia cuenta sólo donde la fila la tiene** (P-36/local D, M-5.3, Q-D1, Q-D2): el fold lee `payload.envelopeArtifactReferenceId` por nombre y la decide por cohorte —clave en la cohorte previa, ausencia en la nueva y presente-inválido (`null`, `""`, número, objeto) se rehúsan por nombre, nunca como ausente (decisión 56)—. La **existencia** (`artifact_reference_read_model` con `artifact_class = 'TASK_ENVELOPE'`) la verifica la puerta de append por nombre antes de escribir y antes del CAS del intento, sin eco del valor; nada en trigger ni FK cruza del stream de tareas al de registro, porque un rebuild vacía las derivadas y repliega un stream a la vez. Alcance, retención y tombstone de la referencia no se verifican. `sameRevisionRecord` es la única comparación de puerta y snapshot: los tres hechos de `canonicalRevision` siempre, y la referencia **sólo si la fila guardada la tiene** —cohorte nueva: otra referencia se rehúsa como escrita una vez, la misma es replay; fila previa `NULL`: un segundo intento estampado tras el upgrade se admite y su referencia queda en el log, no en la fila—. El runtime **carga** la referencia, no la publica (ADR 0080, «carried, not identified»): `InvocationRevision` gana `envelopeArtifactReferenceId` obligatorio en la extensión V2, `deriveInvocation` la proyecta campo a campo, `buildAttemptOpening` la pone en el payload y no entra en ninguna preimagen; el camino V1 sin revisión es byte a byte el de `a6ed7c3` salvo la versión que estampa todo evento. Publicar los bytes del envelope y ligar su referencia en la sumisión es adopción | [ejecución §2](../architecture/database/execution/index.md); [artefactos §4](../architecture/database/artifacts/index.md); [ADR 0080](../../architecture/0080-the-producer-speaks-the-v2-coordinate.md); [ADR 0084](../../architecture/0084-a-revision-names-its-envelope-by-reference-never-by-digest.md) |
| 69 | **El bloque de la intención gana las reglas del stream** (P-36/local D, O-1 de la postauditoría de C): `artifactEventRefusal` corre `SECRET_BEARING` y la lista cerrada de políticas sobre **toda** referencia que un evento lleva, también `payload.intendedReference` de `PUBLICATION_INTENDED`, con las mismas palabras en `payload.intendedReference.classification` y `payload.intendedReference.accessPolicyId`. El fold sigue sin leer el bloque y una intención sin él se admite como antes (decisión 64). Puerta y rebuild llaman la misma función, así que una intención plantada con ese bloque hace fallar el rebuild y el replay de integridad con las palabras de la puerta. N-P36C-20 se replanta: el bloque que la puerta de la intención admite y la del éxito rehúsa es un `artifactReferenceId` que otra publicación ya registró | [artefactos §2 y §10](../architecture/database/artifacts/index.md); [ADR 0083](../../architecture/0083-a-publication-names-its-bytes-only-after-they-survive-the-fsync.md); [ADR 0084](../../architecture/0084-a-revision-names-its-envelope-by-reference-never-by-digest.md) |
| 70 | **La puerta del registry comprueba la versión y el rol; el resolvedor, el transporte y el tiempo** (P-14 A, H-1, H-2, M-4, M-8; precedente decisión 68): `#appendRegistryInTransaction`, tras replay, event id y linaje y antes del INSERT, rehúsa un `ROUTING_ASSIGNMENT_GLOBAL` cuyo `modelVersionId` no nombra una fila `ACTIVE` de `model_version_read_model` o cuyo `role` no está entre sus roles elegibles, con `LedgerValidationError` sin clase nueva y una palabra cerrada a la cabeza del mensaje: `MODEL_VERSION_UNKNOWN`, `MODEL_VERSION_RETIRED` (propone migrar a una versión ACTIVE, sin sucesor), `MODEL_VERSION_DEPRECATED` en `payload.modelVersionId`, `ROLE_NOT_ELIGIBLE` en `payload.role`. Cada fallback, con la misma regla en `payload.fallbacks[i]`, rol incluido. Una asignación que el fold no podría leer se rehúsa campo por campo antes de toda consulta. Sin eco de valores. Un replay exacto de una asignación cuyo modelo se retiró después es replay; otro cuerpo bajo su clave, conflicto de idempotencia. Son consultas tipadas por nombre sobre read models del mismo stream: el stream sigue sin decidir nada (precisión de datos §4 `:236`). El transporte no está en la asignación y no se inventa: lo decide el resolvedor, que lo recibe | [planificación §6](../architecture/database/planning/index.md); [contratos §5](../architecture/contracts/index.md); [ADR 0085](../../architecture/0085-a-role-resolves-from-the-registry-alone.md) |
| 71 | **El payload de `MODEL_VERSION` es cerrado y lo sostiene la puerta; el fold queda total** (P-14 A, M-5): nueve claves, espejo camelCase de cuentas §6 —`provider`, `model`, `release`, `status`, `contextTokens`, `policyVersion`, `deprecatedAt` (null sii `ACTIVE`), `eligibleRoles` (roles de worker, sin repetir), `transports` (`TRANSPORT_KINDS`, sin repetir)—, todas obligatorias y ninguna otra: un rating o un precio no se estaciona en el registry de capacidades. `latest_performance_window` no se lee (columna `NULL`, es de economía). El fold no rehúsa: una fila por `model_version_id`, la de la versión aplicada última en orden de stream, hijas reemplazadas enteras; una versión ilegible de la historia no deja fila y **retira la anterior**, porque una ACTIVE que sobrevive a su sucesora ilegible es el peligro de M-5. La migración 17 crea las tres tablas `STRICT`, sus tres índices y un watermark a la cabeza del registry, sin FK hacia `registry_events` ni hacia routing y sin trigger; los `MODEL_VERSION` ya presentes se pliegan en la misma transacción (`afterSql`, precedente de la 10) con la misma función que la puerta y el rebuild | [cuentas §6](../architecture/database/accounts/index.md); [ADR 0085](../../architecture/0085-a-role-resolves-from-the-registry-alone.md) |
| 72 | **La asignación vigente se lee con el vector al que se leyó** (P-14 A, M-6, E4, N-P14-3): `getGlobalRoutingAssignment({ role, slot })` y `getModelVersion(id)` ejecutan todo dentro de una transacción de lectura diferida —legal bajo `query_only`, instantánea fijada en la primera lectura— y devuelven la respuesta con sus filas de `projection_watermark`: tres para la asignación (`model_version_read_model`@registry y las dos cabezas de routing), una para la versión. Sin asignación vigente, `assignment: null` **con** el vector, porque «nada asignado a este vector» es lo que registra un rechazo. Dos asignaciones vigentes para un mismo `(role, slot)` —dos documentos o dos ramas de uno— se rehúsan con `LedgerQueryError`: nada elige una. Sin reloj, archivo ni append. La regla de rebuild de planificación §6 ya la cumple `rebuildReadModel`, que verifica cabeza, digest y cuenta de los tres streams antes de vaciar una fila | [planificación §6](../architecture/database/planning/index.md); [ADR 0085](../../architecture/0085-a-role-resolves-from-the-registry-alone.md) |
| 73 | **Un rol resuelve desde el registry o no resuelve** (P-14 A, Q5, N-P14-2): `resolveAssignment(request, reading)` en `@acp/accounts` es puro —sin reloj, sin I/O, sin más import que `@acp/contracts`— y su entrada es estructural, mapeada por el llamador de C desde la lectura del ledger. Rehúsa con camino y sin valor: petición fuera de vocabulario; lectura sin vector o de otra coordenada o versión; `ASSIGNMENT_ABSENT` sin default ni caída al archivo de política; `MODEL_VERSION_UNKNOWN`; `MODEL_VERSION_RETIRED` con la propuesta `MIGRATE_TO_ACTIVE_MODEL_VERSION` y nunca un salto silencioso al fallback; `MODEL_VERSION_DEPRECATED`; `ROLE_NOT_ELIGIBLE`; `TRANSPORT_NOT_ADMITTED`; y `ASSIGNMENT_PROVIDER_MISMATCH` si la asignación y su versión discrepan en proveedor. Todo resultado lleva el vector. La resolución **no** lleva el `policyVersion` de la versión: la restricción 6 del registry de capacidades deja toda lectura de una versión de política en el módulo de política, y si el `policy_version` del registry pasa a ser la versión que estampa una ruta es la convergencia de P-28/P-19. `resolveRoute` y `policy/capability-policy.json` quedan intactos como legado | [cuentas §6](../architecture/database/accounts/index.md); [contratos §5](../architecture/contracts/index.md); [ADR 0085](../../architecture/0085-a-role-resolves-from-the-registry-alone.md) |
| 74 | **La petición no es el payload, y una iniciativa se registra por una sola orquestación** (P-14 B, H-1, H-2, V3 §3.d, §3.e): `registerInitiative` en `@acp/ledger` es lo único que decide, publica y apendea un registro; la ruta `POST initiatives` y el verbo `acp initiative` abren los handles, acuñan los ids —nunca el de la iniciativa— y mapean. La petición se valida con `Initiative` completo, guards sobre el objetivo antes de publicar; `contractVersion`, `status: ACTIVE` y `createdAt` los compone el llamador. El payload de `INITIATIVE_REGISTERED` es cerrado —`slug`, `title`, `objectiveSha256`, `objectiveArtifactReferenceId`— y nunca lleva `objective`; lo sostiene la orquestación, no la puerta del stream, y `InitiativeEvent` no cambia (contracts intacto, 2.5.0). Clave `initiativeId/1/register`; la comparación es precondición de contenido contra el evento registrado —slug, título, digest del objetivo—: igual es replay sin publicar ni apendear; distinto, `CONFLICT` con `at` en el campo. Una carrera perdida en el append re-lee y re-decide. Las dos puertas imprimen `InitiativeRegistrationResponse`; sin código de error nuevo | [contratos §5](../architecture/contracts/index.md); [ADR 0086](../../architecture/0086-an-initiative-enters-by-command-and-by-api.md) |
| 75 | **El objetivo va al plano privado bajo claves derivadas, y un reintento reanuda la única publicación** (P-14 B, H-4, V3 §3.a, §3.b; precedente Q2): `PLAN_DOCUMENT`, `INTERNAL`, scope `INITIATIVE`/`initiativeId`, `PERMANENT`, `SCOPE_EQUALITY_V1`, `text/plain; charset=utf-8`, `PLAINTEXT`; una clase nueva sería versión de contrato. Claves `initiativeId/objective/<digest>/intended` y `…/succeeded`; orden decisión → publicación → append. El reintento busca la intención por su clave y reutiliza sus identidades, instantes y referencia, e ignora los ids frescos. Holding `holder = recordedBy`, `holderPid` de la puerta, cinco minutos informativos. Aceptado por nombre: un objetivo de una iniciativa tiene exactamente una publicación; una pareja ABANDONED se rehúsa `PUBLICATION_ALREADY_ABANDONED` hasta otra decisión; un crash entre publicación y append deja una referencia sin iniciativa hasta el reintento; una tenencia de un proceso muerto no se desplaza (`QUIESCENCE_UNPROVEN`), y su reconciliación no es de B | [artefactos §8](../architecture/database/artifacts/index.md); [ADR 0086](../../architecture/0086-an-initiative-enters-by-command-and-by-api.md) |
| 76 | **Migración 18, lectura por referencia y la quinta puerta de escritura** (P-14 B, M-6, M-7, H-3, V3 §3.c, §3.f; planificación §1 `:23-25`): tres `ADD COLUMN` nullable en `initiative_read_model` —`title`, `objective_sha256`, `repository_sha256` sin productor— y re-plegado del stream en `afterSql`; ni watermark, ni objeto de schema, ni `LEDGER_CONTRACT_VERSION` se mueven. `readInitiativeObjective` lee el objetivo con `plane.read` bajo el scope de la iniciativa sólo para la cohorte del payload cerrado, tras ver la raíz, con un lease store que rehúsa toda tenencia; lectura rehusada es `LEDGER_INTEGRITY`, nunca `objective: null`; el DTO no cambia. `API_WRITE_ROUTES` 4 → 5 por `registerGetAndPost` (bearer heredado), `API_CONTRACT_VERSION` 0.15.0 → 0.16.0, `SURFACE_MAP` gana `initiative` → `initiatives` POST `DOCUMENT`; el verbo usa `openForWrite` y `readOperatorDocument` exportado, y L-B4B-11 no se mueve. Ley L-P14B-1: ningún `src/` asigna un `initiativeId` desde `randomUUID(` o un literal uuid, y un solo módulo construye un `INITIATIVE_REGISTERED` | [planificación §1](../architecture/database/planning/index.md); [ADR 0086](../../architecture/0086-an-initiative-enters-by-command-and-by-api.md) |
| 77 | **La clave del cliente tiene un solo hogar: `task_submission_read_model`, plegada del ingreso** (P-14 C, Q1, H-1; contratos §15): la migración 19 crea la tabla `STRICT` —`client_scope`, `client_request_key`, `task_id`, `revision_number`, `envelope_sha256`, `sequence`, `created_at`— con `UNIQUE (client_scope, client_request_key)`, sin trigger ni FK, un watermark sembrado a la cabeza del stream de tareas y re-plegado en `afterSql`; entra en `DERIVED_TABLES`, `PROJECTION_NAMES`, `PROJECTION_SOURCES` y `EXPECTED_SCHEMA_OBJECTS` (`status().projections` 18 → 19). Insert-only por el fold: tarea, número de revisión y digest salen del mismo registro de revisión; misma clave y misma fila es replay sin escritura; otra fila se rehúsa con `LedgerIdempotencyConflictError` por nombre en puerta y snapshot, nunca `SqliteError` ni `ON CONFLICT DO UPDATE`. Lectura `getTaskSubmission`. `TaskReadModel` gana `stepId`, `role` y `commitPolicy`, escritos una vez por el ingreso y arrastrados. El envelope no toca contracts: los campos que no son trabajo viajan al lado, `CONTRACT_VERSION` 2.5.0 | [ejecución §1 y §1.1](../architecture/database/execution/index.md); [contratos §15](../architecture/contracts/index.md); [ADR 0087](../../architecture/0087-a-task-enters-once-by-its-clients-key.md) |
| 78 | **El ingreso es un `TASK_DISCOVERED` bajo su propia transición, y el productor propone lo que el ledger verifica** (P-14 C, H-2, H-4, M-7, M-9): `transitionId` `intake` —nunca `discovered`, que es de la discovery del daemon con su digest de sumisión—, `fromState: null`, clave V2 `<taskId>/1/1/intake`, `attempt: 1`, sin causación ni correlación, `emittedBy = recordedBy`. Payload cerrado: registro de revisión, `initiativeId`, clave del cliente, vínculo al roadmap como par, rol, `commitPolicy` y `resolution` con `slot` y el vector leído; el fold es total. Orden de rechazos: forma (`REQUEST_INVALID`) → clave (replay o `CONFLICT` en `envelope`, `roadmapVersionId`, `stepId`, `role`, `slot`, `transportKind`) → tarea ya ingresada por otra clave (`CONFLICT` en `envelope.taskId`) → iniciativa inexistente (`REQUEST_INVALID`) → versión de roadmap ajena → rol fuera del envelope → resolución de A (`ASSIGNMENT_REQUEST_INVALID`/`READING_INVALID` → `REQUEST_INVALID`, las otras siete → `AUTHORITY_REFUSED`, RETIRED con propuesta). Carrera perdida re-lee y re-decide. `latest_attempt_number` 1 sin fila de intento, declarado. Deuda nombrada: la existencia del paso dentro del documento (P-26) y la autoridad del envelope contra el árbol (P-23); la apertura del intento de P-15 parte de `DISCOVERED` (enmienda a ADR 0080 §4 nombrada) | [contratos §5](../architecture/contracts/index.md); [ADR 0080](../../architecture/0080-the-producer-speaks-the-v2-coordinate.md); [ADR 0087](../../architecture/0087-a-task-enters-once-by-its-clients-key.md) |
| 79 | **El envelope va al plano privado, la sexta puerta de escritura y el ingreso no adquiere** (P-14 C, Q2, Q3, M-6, M-8, L-10): bytes `canonicalJsonStringify(TaskEnvelope.parse(value))`, `application/json; charset=utf-8`, `PLAINTEXT`; dos digestos distintos —`envelope_sha256` de la preimagen de identidad y `contentSha256` de los bytes—; `TASK_ENVELOPE`, `INTERNAL`, scope `TASK`/`taskId`, `PERMANENT`; claves `taskId/envelope/<envelope_sha256>/intended` y `…/succeeded`; orden decisión → publicación → append; reintento reutiliza la intención; aceptados por nombre la referencia sin tarea tras un crash y la pareja ABANDONED rehusada. Ninguna puerta lee el envelope. El ingreso no lee el grafo, no toma lease ni ejecuta. `intakeTask` vive en `@acp/runtime` porque importa accounts. `API_WRITE_ROUTES` 5 → 6 (`tasks` POST por `registerGetAndPost`, bearer heredado), `API_CONTRACT_VERSION` 0.16.0 → 0.17.0, `SURFACE_MAP` gana `intake` → `tasks` POST `DOCUMENT`, 400 `BAD_REQUEST` / 409 `WRITE_REFUSED` con clase, código y propuesta; `acp intake --request` por `openForWrite`, L-B4B-11 quieto. Ley L-P14C-1: ningún `src/` asigna un `taskId` desde `randomUUID(` y un solo módulo construye un evento bajo la transición del ingreso | [artefactos §8](../architecture/database/artifacts/index.md); [ADR 0087](../../architecture/0087-a-task-enters-once-by-its-clients-key.md) |
| 80 | **La liquidación de uso es un fold puro con entrada y salida cerradas, y lo que suma y lo que compite queda fijado** (P-32/captura A, H-3, H-4, H-5, H-6, H-7): `foldUsageSettlement` en `@acp/ledger` recibe corte (efecto y cabeza de control), disparador (secuencia e instante), flujos con su `source_class`, observaciones con la forma de §1.2, revisión anterior, `lastFinalSequence`, política y `foldVersion`; devuelve header de §2.1 con los cinco conteos `bigint | null`, vector con sólo la fila de control, todas las observaciones consideradas por secuencia y la elección por segmento, o `{ ok: false, reason, at }` con `USAGE_SETTLEMENT_REFUSALS` cerrado y ordenado —DISPUTED no es rechazo—. La clase es del flujo; la unidad de competencia es el linaje `(source, account_id, route_segment_id)`, cuyos epochs suman; dentro de un segmento gana la clase más alta, igual clase con las cuatro sumas iguales elige el menor `measurement_stream_id` y distintas dan DISPUTED con cinco NULL; entre segmentos suman; FINAL exige cada flujo elegido sin huecos y con `is_final = 1` efectivo. Dentro de un flujo se lee por ordinal: la cadena de correcciones da los valores de la última y la cobertura de la raíz, bifurcación `CORRECTIONS_FORKED`, DELTA que toca un rango efectivo o solape parcial o CUMULATIVE estrictamente dentro `COVERAGE_OVERLAP`, CUMULATIVE que contiene entero reemplaza. Total = suma BigInt de cuatro clases (`TOTAL_MISMATCH`), techo int64 (`TOKENS_OVERFLOW`). `had_late_arrival` sólo por secuencia posterior a la última FINAL. Declaradas: `OBSERVATION_DUPLICATE`, `REQUEST_INVALID`, `STREAM_DUPLICATE`, `STREAM_SOURCE_CLASS_INVALID` y `STREAM_LINEAGE_CLASS_MIXED` sobre el piso de la preauditoría | [economía §1–2](../architecture/database/economy/index.md); [ADR 0088](../../architecture/0088-a-settlement-fold-never-invents-a-number.md) |
| 81 | **La identidad del flujo es una preimagen versionada y la política es un documento literal pineado** (P-32/captura A, H-1, H-2, Q4; clase de la decisión 45): `measurementStreamPreimageV1` = `acp/usage-measurement-stream/v1` con un LF + JSON canónico del array posicional `[source, accountId, routeSegmentId, sourceEpoch]`, sin separador; `measurementStreamIdV1` su SHA-256; valida por nombre antes de hashear y rehúsa con `LedgerValidationError` `STREAM_COORDINATE_INVALID`; nunca el id de conexión del proveedor; sólo el canonicalizador y el sha-256 del paquete. `USAGE_SOURCE_POLICY_V1` congelado, `USAGE_SOURCE_POLICY_SHA256_V1` derivado y pineado por el test, `USAGE_FOLD_VERSION_V1 = 1`, en `@acp/ledger`; el fold rehúsa otra política (`POLICY_UNSUPPORTED`) u otra versión (`FOLD_VERSION_UNSUPPORTED`). Un cambio de regla es documento, digest y versión nuevos al lado; nada de `CONTRACT_VERSION` se mueve en A | [economía §1–2](../architecture/database/economy/index.md); [ADR 0088](../../architecture/0088-a-settlement-fold-never-invents-a-number.md) |
| 82 | **A es inerte por ley; la exposición es input en A y evento en B** (P-32/captura A, H-8, Q3): L-P32A-1 —ningún `src/` fuera del módulo y del barrel de `@acp/ledger` nombra `usage-settlement/index.js`, `foldUsageSettlement`, `measurementStreamIdV1` ni `measurementStreamPreimageV1`— y L-P32A-2 —el módulo no nombra `Date.now(`, `new Date(`, `process.env`, `Math.random(`, `node:crypto` ni `createHash(`, e importa de `../canonical-json/index.js`—; `PATH_SCOPED_LAWS` 140 → 142. B retira L-P32A-1 al cablear la puerta y lo nombra en su brief. En A la exposición del efecto es un disparador sin observaciones (revisión UNKNOWN); en B el primer `DISPATCH_INTENDED` la produce. Sin migración, tabla, tipo de evento ni puerta; `TOKEN_USAGE_RECORDED`, rollups y quota intactos | [ADR 0088](../../architecture/0088-a-settlement-fold-never-invents-a-number.md) |
| 83 | **El uso entra con dos tipos de evento y una sola función de captura, y el settlement se escribe en la transacción del disparador** (P-32/captura B, Q1, H-3, H-4, H-6, H-7, H-11): `USAGE_STREAM_DECLARED` y `USAGE_OBSERVATION_RECORDED`, al final del vocabulario (33 → 35), passthrough de mismo estado exigido por la puerta, en el canal `progress` (2 → 4; `execution` queda 15), con payload cerrado —coordenada V2 y un record, `USAGE_STREAM_RECORD_KEYS` o `USAGE_OBSERVATION_RECORD_KEYS`—. La puerta recomputa `measurementStreamIdV1`, exige el segmento y su intento, el efecto de la tarea y coordenada del evento y el mismo intento para el segmento del flujo, identidad de reporte única, corrección del mismo flujo y efecto, todo por nombre y nunca por aborto de FK. `nextUsageCapture` es la única función de la puerta (`#projectEvent`), del rebuild (`applyEventToSnapshot`) y de la migración 20: pliega con el fold de A con `cut.controlHead` = secuencia y digest del propio disparador; los rechazos del fold se elevan como `LedgerValidationError` dentro de la transacción del append. Un reporte no final tras uno final se admite (tardío, `had_late_arrival`); sólo una CORRECTION retira el final. `verifyIntegrity` compara las cinco tablas como texto con `safeIntegers`. L-P32A-1 se retira; L-P32B-1 admite sólo el módulo, el barrel, `ledger/index.ts` y `projection/index.ts`; `PATH_SCOPED_LAWS` queda 142 | [economía §1–2](../architecture/database/economy/index.md); [ADR 0089](../../architecture/0089-usage-is-a-declared-stream-and-a-measured-observation.md) |
| 84 | **La exposición de un efecto es su revisión 1, y «primer» significa «sin revisión»** (P-32/captura B, Q3, H-1, H-5): un `DISPATCH_INTENDED` de un efecto sin revisión pliega la revisión 1 `UNKNOWN` —cinco NULL, lista vacía, `last_observation_id` NULL, `computed_at` y `sequence` de la entrega—; uno de un efecto con revisión no escribe nada, nunca decidido por `attemptOrdinal`. Una observación de un efecto sin entrega se rechaza por nombre. La migración 20 crea las cinco tablas de economía §1.1, §1.2 y §2.1–2.3 con CHECK/UNIQUE/INDEX literales, FK diferidas (la autorreferencia de la observación sin RESTRICT), forma de digest de §3.4, cinco watermarks a la cabeza, y en `afterSql` pliega el stream de tareas con la misma función, de modo que todo efecto ya entregado tiene su exposición en su primera entrega | [economía §1–2](../architecture/database/economy/index.md); [ADR 0089](../../architecture/0089-usage-is-a-declared-stream-and-a-measured-observation.md) |
| 85 | **2.6.0 lo paga una identidad verificable, no un digest de envelope** (P-32/captura B, H-8, H-12; criterio de ADR 0076): `CONTRACT_VERSION` `2.5.0` → `2.6.0` y `SUPPORTED_CONTRACT_VERSIONS` cinco miembros, porque la puerta recomputa `measurement_stream_id` desde su preimagen versionada y cada declaración trae `normalization_policy_sha256`. `envelope_sha256` no está en ningún payload de B. Los tres vectores de envelope-identity se mueven con la versión (consecuencia V3), computados dos veces; la fixture de telemetry queda en `2.2.0`; `API_CONTRACT_VERSION` y `LEDGER_CONTRACT_VERSION` derivada no se tocan salvo su pin | [economía §1–2](../architecture/database/economy/index.md); [ADR 0089](../../architecture/0089-usage-is-a-declared-stream-and-a-measured-observation.md) |
| 86 | **El productor de uso registra lo que el adapter ya normalizó, y un reinicio lee su generación en vez de reinventarla** (P-32/captura C, Q5, H-1, H-2 (a), H-3, H-5, H-6, H-7): `recordUsageStreamDeclaration` y `recordUsageObservation` en `@acp/runtime` exigen `invocation.revision` y rehúsan por nombre una invocación V1 antes de armar nada; la coordenada del payload sale de la revisión, nunca de `invocation.attempt`. El id del flujo es `measurementStreamIdV1` importado; claves `usage-stream.<id>` y `usage-observation.<id>.<ordinal>`, sin reloj ni generación de aterrizaje; instantes del sobre = `submittedAt`, `occurredAt` del record = el del adapter. El recorder rehúsa sólo tarea desconocida, invocación sin revisión, `sourceClass` fuera de `USAGE_SOURCE_CLASSES` y `reportKind` fuera de `USAGE_REPORT_KINDS`; lo demás pasa verbatim y lo decide la puerta. `readUsageStreamLineage` lee el mayor epoch declarado de `(source, accountId, routeSegmentId)` del stream de eventos por `UsageEventSource`, exhaustivo o rechazo (`LINEAGE_SCAN_INCOMPLETE`, `LINEAGE_DECLARATION_UNREADABLE`), y responde `latest: null`, nunca 0; sin verbo nuevo en el ledger. L-P32B-1 se enmienda en su fila (la identidad admite el recorder del runtime, el fold no) y L-P32C-1 mantiene los recorders sin cableado hasta P-15; `PATH_SCOPED_LAWS` 142 → 143. `recordTokenObservation`, rollups y quota intactos; sin bump | [economía §1](../architecture/database/economy/index.md); [ADR 0090](../../architecture/0090-a-usage-recorder-reports-what-the-adapter-normalized.md) |
| 87 | **El catálogo de precios es la tabla de economía, sin índice duplicado, y su fold vive en cuatro sitios** (P-33/catálogo A, Q5, H-1, H-2, H-5): la migración 21 `price_interval_catalog` crea `price_interval_read_model` `STRICT` —el nombre de la tabla y del watermark es el del diccionario; `price_interval_catalog` es sólo el de la migración— con las doce columnas, la PK de ocho de `:205`, `ck_price_interval_read_model__token_class`, `__interval_order`, `__price_per_million_nanos`, `__currency`, `__catalog_version` y `__sequence`, sin FK ni trigger, y un watermark sembrado a la cabeza del registry. **No se crea `ix_price_interval_read_model__lookup`**: nombra las ocho columnas de la PK en su orden y el autoindex de la PK ya es ese índice; desviación registrada contra economía `:206`, `EXPECTED_SCHEMA_OBJECTS` +1. `nextPriceIntervalProjection` es total y entera por versión —un payload ilegible pliega cero filas de esa versión, nunca parte, nunca throw— e insert-only (una versión retroactiva no toca las filas de otra), con un solo escritor, y corre en la puerta dentro de la transacción del append, en el `afterSql` de la 21, en el rebuild (la tabla está en `DERIVED_TABLES`) y en `verifyIntegrity`, que compara fila a fila sin imprimir precios. `readPriceIntervals` lee exacto por documento y versión, en orden de PK, `[]` si no hay filas. Sin bump: `PRICE_TABLE` ya estaba en ambos CHECK | [economía §3](../architecture/database/economy/index.md); [ADR 0091](../../architecture/0091-a-price-interval-is-published-whole-by-document-and-version.md) |
| 88 | **La clave de solape es la PK menos `effective_from`, y el payload de un `PRICE_TABLE` es cerrado** (P-33/catálogo A, Q2, Q4, H-4, H-7, H-8): payload `{ intervals }`, lista no vacía, ocho claves por intervalo (`provider`, `modelVersionId`, `transportKind`, `tokenClass`, `currency`, `effectiveFrom`, `effectiveTo` presente como null o instante, `pricePerMillionNanos`), instantes en la forma canónica con round-trip, precio entero seguro `>= 0`, transporte de `TRANSPORT_KINDS`, moneda `^[A-Z]{3}$` en puerta y CHECK (espejo de ejecución `:682`, sin lista ISO). Dentro de una versión no se tocan dos intervalos de la quíntupla `(provider, model_version_id, transport_kind, token_class, currency)` de economía `:190-191`; base de datos `:670` abrevia la regla omitiendo el transporte y se sigue economía porque la PK y el precio distinguen transporte. `PRICE_INTERVAL_DUPLICATE` (misma PK) y `PRICE_INTERVAL_OVERLAP`, fail-closed antes de admitir; intervalos adyacentes `[a, b)` y `[b, c)` no se tocan. Inline bajo los 64 KiB del registry: unos doscientos intervalos caben, cuatrocientos no | [economía §3](../architecture/database/economy/index.md); [datos §13](../architecture/database/index.md); [ADR 0091](../../architecture/0091-a-price-interval-is-published-whole-by-document-and-version.md) |
| 89 | **La existencia del modelo es de la puerta, en cualquier estado y bajo su proveedor; el fold sólo lee la forma** (P-33/catálogo A, Q3, H-3, H-11 opción a): `#assertRegistryDocumentAdmissible` despacha en tres ramas —`MODEL_VERSION`, `PRICE_TABLE` y el resto a la compuerta de asignaciones— y `PRICE_TABLE` nunca cuelga del `else`. `priceTableIssues` es híbrida: forma pura primero, luego un lookup inyectado de `model_version_read_model`; cada `modelVersionId` registrado en cualquier estado (un RETIRED conserva precio histórico) o `MODEL_VERSION_UNKNOWN`, y registrado bajo el `provider` del intervalo o `MODEL_VERSION_PROVIDER_MISMATCH`. Sin cruce de transporte (es del resolver). El fold, el `afterSql`, el rebuild y la réplica de integridad no re-verifican existencia (precedente N-P14A-7): N-P33-12 se lee «pliega la forma» | [economía §3](../architecture/database/economy/index.md); [ADR 0091](../../architecture/0091-a-price-interval-is-published-whole-by-document-and-version.md) |
| 90 | **Una declaración nueva vive en la hoja semántica, y la excepción inline del ADR 0088 queda retirada** (P-33/catálogo A, C-1, C-2, C-3 del brief v3, sobre H1 de la auditoría de reinicio 2026-09-14): la ley del owner (`docs/audit/architecture/index.md` §7) manda toda `interface`/`type`/alias de un concepto a su hoja `types/index.ts`, privadas incluidas, y prohíbe declaraciones con nombre intercaladas con la implementación. `PriceTableRefusal` y `PriceTableModelVersion` salen de `src/projection/index.ts`, `PriceIntervalRow` sale de `src/ledger/index.ts`, y las tres quedan en `src/types/index.ts` junto a los row types que ya viven ahí; el vocabulario viaja con su unión derivada (`PRICE_TABLE_REFUSALS` a la hoja, al lado de `PRICE_TOKEN_CLASSES` y las keys), porque la derivación de §7.1 es de una sola dirección y una hoja no importa un valor de la implementación que tipa. Superficie publicada sin cambio: el paquete sólo exporta su barrel. La línea «Types live inline in the module (H-11)» del ADR 0088 queda **SUPERSEDED** para toda declaración nueva por una errata fechada 2026-09-14 en ese ADR —la aceptación de un brief o de un auditor no enmienda una ley del owner— sin reescribir ninguna otra línea suya y sin cambiar su estado. Las declaraciones inline preexistentes de `usage-settlement`, `artifact-plane`, `artifact-lease-store`, `initiative-registration`, `assignment` (accounts) e `intake` (runtime) quedan como **deuda nombrada**: su corrección mecánica acotada es un lote propio con write-set propio (C-3), abierto tras el cierre de A, porque mezclarla violaría la atomicidad del lote. El write-set de A pasa a dieciséis rutas: las quince congeladas más el ADR 0088, sólo para la errata | [arquitectura §7](../architecture/index.md); [ADR 0088](../../architecture/0088-a-settlement-fold-never-invents-a-number.md); [ADR 0091](../../architecture/0091-a-price-interval-is-published-whole-by-document-and-version.md) |
| 91 | **La deuda que la errata del 0088 nombró queda pagada en seis conceptos, y en ninguno más** (C-3 / P-37 seam 1, `C3_TYPES_SEAM1_ADJUDICATED` 2026-09-14; ejecuta la deuda registrada en la fila 90): las 90 declaraciones `interface`/`type` de `usage-settlement` (24), `artifact-plane` (18), `artifact-lease-store` (15), `initiative-registration` (11), `intake` (12) y `assignment` (10) —exportadas y privadas— pasan a la hoja `<concepto>/types/index.ts` de su propio concepto, molde `outbox-store/types/` y `projection/types/` (corrección `a6ed7c3`). Cada módulo importa con `import type` lo que usa y re-exporta desde la hoja **todos** los nombres que ya exportaba, así que la superficie queda estable nombre por nombre y los barrels de paquete no se tocan (verificado: `git diff` vacío en los 13 `packages/*/*/src/index.ts`); las 15 declaraciones antes privadas se exportan sólo de su hoja y quedan deliberadamente fuera de esa re-exportación, de modo que siguen sin exportarse del paquete (§7.2). Los valores no se mueven: los conjuntos cerrados se quedan junto a la implementación y las hojas los leen type-only, que es la derivación de un solo sentido de §7.1. **Puramente mecánico**: nada se renombra, ninguna firma cambia, ningún literal de implementación se toca, ningún test se toca y no hay ADR nuevo porque no hay decisión de diseño; ningún pin se mueve (`MIGRATIONS` 21, `CONTRACT_VERSION` `2.6.0`, `PATH_SCOPED_LAWS` 143 quietos) porque un tipo se borra al emitir. La reorganización masiva sigue excluida por mandato del owner: los conceptos fuera de estos seis se reportan, no se tocan. **Adjudicación v2** (ante el `NEEDS_DECISION` del writer, opción A): la **ley de aislamiento se hace cumplir sobre el concepto —el `index.ts` del módulo más su hoja `types/index.ts`—, no sobre el archivo solo**, y queda como molde para los seams restantes de P-37. Cuatro suites pinean el aislamiento de un concepto como allowlist exacta de especificadores `from "…"` leída de su fuente (`artifact-lease-store`, `artifact-plane`, `assignment`, `intake`); mover declaraciones a la hoja no sacó nada del concepto, pero sí sacó una dependencia type-only del archivo que esos pines leen —el `../ledger/index.js` de `artifact-plane` vive ahora en su hoja—, de modo que el pin medía el contenedor equivocado. Cada allowlist queda como estaba más la hoja hermana, y nada ajeno se permite de nuevo; re-pinear sólo la lista del módulo se rechazó como debilitamiento silencioso, porque ninguna ley del fence pinea los imports de ese módulo (sus leyes path-scoped son filesystem, reloj e identidad). Write-set 14 → 18: se agregan los cuatro archivos de test, y `P37S1_WRITE_SET` los registra | [arquitectura §7](../architecture/index.md); [ADR 0088, errata](../../architecture/0088-a-settlement-fold-never-invents-a-number.md) |
| 92 | **El veredicto de precio tiene dos miembros y no lleva importe** (P-33/catálogo B, E7/E13, economía §3 `:195-197` y `:284-286`): `PriceResolution = { status: "FOUND", interval } | { status: "PRICE_MISSING", pin }`, sin tercer miembro y sin campo de importe que pueda valer cero —la prohibición de la tarifa de respaldo `0` queda expresada en el tipo, no en un comentario—. `FOUND` lleva la fila entera, así que el precio, su moneda y la ventana de la que se leyó viajan juntos; `PRICE_MISSING` lleva el pin reconstruido campo por campo (nunca un alias del objeto del llamador) y nada más, que es la frase de `:284-286` como valor: documento y versión se conservan, la referencia de intervalo queda vacía. `PRICE_MISSING` se escribe como lo escribe el estado de valuación de economía §4, para que el ledger y el diccionario no deriven en dos nombres para un mismo resultado. `PricePin` son las dos columnas que eligen versión y `PriceKey` las cinco que eligen fila; con el `effectiveFrom` que la resolución encuentra son las ocho de la PK, de modo que la identidad es la misma que nombra estimación `:286`, partida por donde el mapa cortó A de B | [economía §3](../architecture/database/economy/index.md); [estimación](../architecture/contracts/estimation/index.md); [ADR 0092](../../architecture/0092-a-price-is-found-inside-its-pinned-catalog-version.md) |
| 93 | **La resolución selecciona dentro de la versión fijada, no re-admite, y se alcanza sólo por el barrel** (P-33/catálogo B, E3/E8, Q1, L-P33B-1): `resolvePrice(intervals, pin, key, instant)` es **pura** —sin base, sin reloj, sin identidad, sin I/O— y decide en este orden: `modelVersionId` null → `PRICE_MISSING` **nunca** aliasado a otra versión de modelo (guardado dos veces a propósito: antes del barrido y como la comparación de fila, para que ninguna sea la única que sostiene); documento y versión antes que nada, así que una fila de otra versión no es candidata aunque esté vigente (`:205`); la quíntupla completa y exacta, sin fallback entre moneda, transporte ni clase de token (la moneda es identidad y no se convierte); y la ventana **semiabierta** `effectiveFrom <= instant < effectiveTo`, con `effectiveTo` null = sin fin declarado, de modo que en la frontera entre adyacentes exactamente uno cotiza el instante y es el posterior (estimación `:291`). **No re-admite**: no re-verifica solape, forma ni existencia del modelo —precedente N-P14A-7— porque la puerta de A ya lo decidió y un resolvedor que re-juzgara filas almacenadas sería una segunda puerta más débil; un pin malformado no necesita rama, no coincide con nada. **L-P33B-1**: ningún `src/` fuera del concepto (módulo **y** su hoja, molde de C-3 / P-37 seam 1, fila 91) ni del barrel nombra el verbo; la ley **no admite ningún llamador** todavía, porque el pin no tiene hogar físico hasta P-15 (Q1). `PATH_SCOPED_LAWS` 143 → 144. Sin bump: son hechos sobre lo que A publicó (criterio 0076). Registrada como frontera: el instante del llamador **no** se valida aquí, porque la gramática no tiene hogar importable y restituirla sería un segundo codificador; P-15 lo hace comprobable | [economía §3](../architecture/database/economy/index.md); [ADR 0091](../../architecture/0091-a-price-interval-is-published-whole-by-document-and-version.md); [ADR 0092](../../architecture/0092-a-price-is-found-inside-its-pinned-catalog-version.md) |
| 94 | **El contenido de una instrucción es una lista ordenada de bloques discriminados, y una sola forma para los tres clientes** (P-06/A, E1-E8/E10-E12, Q1): `InstructionContent = { contentContractVersion: 1, blocks }` y nada más; bloque `strictObject` con las nueve claves **presentes** —`kind` de los cinco cerrados, `blockId` estable y único, `mediaType` validado **contra la clase** desde una tabla (no un formato: `image/png` es correcto y es el equivocado para `audio`), `byteLength` declarado = el real y dentro del techo del perfil, `contentSha256` de los bytes, `artifactRefId` obligatorio salvo texto corto, y `text`/`toolCallId`/`effectId` en null en las clases a las que no pertenecen, presentes y no ausentes (precedente `effectiveTo`)—. **«Texto corto» = 4.000 caracteres** (`CONTENT_INLINE_TEXT_MAX_CHARS`, Q1): **nombra** el bound que `objective` e `instructions` ya aplican, no inventa una segunda política. Tres reglas son de la lista y no del bloque: `blockId` nombra un bloque, al menos uno es `text` (una instrucción dice algo) y el **agregado del pedido** se rechaza aunque cada bloque quepa. Sobre un tope es **rechazo, nunca truncación** (precedente literal de `ExecutionRequest.instructions`). `CONTENT_BLOCK_REFUSALS` nombra diez razones, sin eco de valores y con el guarda de credenciales del paquete adjunto: **ocho viajan a la cabeza del mensaje** y **dos las hace cumplir la forma misma** —`BLOCK_KIND_UNKNOWN` por el `z.enum` cerrado de `kind`, `VENDOR_FIELD_PRESENT` por `strictObject`—, de modo que se rechazan antes de cualquier refinamiento y afloran bajo los códigos propios de zod (`invalid_value` en `blocks.<i>.kind`, `unrecognized_keys` en `blocks.<i>` nombrando las claves). El vocabulario las lista igual porque **la razón es parte del contrato aunque la palabra no viaje en ningún mensaje**, y la suite pinea cada una al código de zod que realmente produce. **No se agrega un error map** para uniformar el vocabulario: sería una segunda capa de traducción que mantener al día con los códigos de zod, sin ningún llamador que la pida todavía. Consecuencia declarada: el techo de artefacto (8 MiB) y el agregado (8 MiB) coinciden, así que un artefacto al máximo nunca viaja —el bloque `text` obligatorio empuja el total— y la suite afirma **cuál** de las dos palabras vuelve, porque eso es la evidencia de que las dos reglas son independientes | [contratos §4.1](../architecture/contracts/index.md); [tests §9.5](../quality/testing/index.md); [ADR 0093](../../architecture/0093-an-instructions-content-is-an-ordered-list-of-discriminated-blocks.md) |
| 95 | **Todo tipo de un recurso del kernel se declara en su hoja, y el valor se renombra para que eso sea posible** (P-06/A, adjudicación **v3**, que SUPERA la v2; patrón para los futuros módulos de schema del kernel): la hoja `content-block/types/index.ts` aloja **las cinco** declaraciones del concepto —las tres uniones derivadas `ContentBlockKind`, `ContentMediaType` y `ContentBlockRefusal`, más `ContentBlock` e `InstructionContent`, cada uno `z.infer` de su schema—, que es la fila `kernel` de §7.1 al pie de la letra. Por eso los valores se llaman `ContentBlockSchema` e `InstructionContentSchema`: **un valor y un tipo no pueden compartir un nombre entre dos archivos** (`TS2323` si el módulo re-exporta el tipo de la hoja bajo el nombre de su const, `TS2300` si un barrel fusiona valor-de-un-archivo con tipo-homónimo-de-otro, `TS2440` dentro de la hoja), así que se renombra el **valor** y la fusión no llega a existir. Las sondas del writer demuestran exactamente eso —una restricción del **nombre fusionado**, no la inseparabilidad del concepto—: la v2 las sobreleyó como si §7.2 concediera una excepción a §7.1, y §7.2 gobierna la dirección de la dependencia y los ciclos, no concede tal excepción. La hoja importa los schemas type-only y el módulo no importa ningún valor de la hoja, así que no hay ciclo en runtime; el módulo re-exporta los cinco tipos, de modo que los importadores siguen leyendo del módulo. **Alcance: sólo este concepto** — los 62 nombres fusionados históricos del paquete no se tocan, no es reforma general y no reabre C-3 / P-37. Se rechazaron el tipo base a mano con `satisfies` (crea **dos autoridades sobre una forma**) y la opción sin hoja («no se crean carpetas vacías» no aplica cuando la hoja tiene contenido real). **L-P06A-1** mantiene el contrato inerte —ningún `src/` fuera del concepto (módulo **y** hoja) ni de los dos barrels lo nombra— sin admitir **ningún** llamador hasta B, y su patrón se amplió a los nombres renombrados porque `\bInstructionContent\b` no alcanza a `InstructionContentSchema`: sin eso la ley habría quedado ciega en silencio tras el renombre. `PATH_SCOPED_LAWS` 144 → 145 (la ley se enmienda en su fila, no crece); `CONTRACTS_SCHEMA_EXPORTS` +18; sin bump (el contenido entra al envelope en B, y B mueve `CONTRACT_VERSION`) | [arquitectura §7](../architecture/index.md); [ADR 0088, errata](../../architecture/0088-a-settlement-fold-never-invents-a-number.md); [ADR 0093](../../architecture/0093-an-instructions-content-is-an-ordered-list-of-discriminated-blocks.md) |
| 96 | **El envelope lleva el contenido, y `objective` queda atado a su primer bloque de texto** (P-06/B, E16, ADR 0093): `TaskEnvelope` gana `content: InstructionContentSchema` **requerido** e **importado** —A es la única autoridad sobre la forma de un bloque y una copia aquí sería una segunda—; opcional se rechazó porque un campo opcional es la segunda autoridad que el 0093 refusó y un envelope que ninguna puerta admitiría no debe ser construible. Entra en la preimagen de `envelope_sha256` **por construcción**: la preimagen es el JSON canónico del envelope parseado entero, así que ningún archivo enumera los campos dos veces y `envelope-identity` no necesitó cambio — cambiar un bloque cambia la revisión (N-P06-10). `objective` **se queda** y el schema rehúsa que difiera del texto del primer bloque `text`: dos grafías de un hecho que no pueden discrepar, que no es la coexistencia del 0093 porque ahí cada campo podía decir algo y aquí el productor es rechazado por nombre. Se queda por una razón declarada: B no puede tocar `instructionFor`, y quitar el campo que lee sería tocarlo; C lo retira al mover el productor | [contratos §3-§4](../architecture/contracts/index.md); [ADR 0093](../../architecture/0093-an-instructions-content-is-an-ordered-list-of-discriminated-blocks.md); [ADR 0094](../../architecture/0094-the-task-envelope-carries-the-instructions-content.md) |
| 97 | **Las dos puertas son una, y la puerta verifica las referencias sin publicarlas** (P-06/B): CLI y gateway llegan al ingreso por el mismo `intakeTask` de `@acp/runtime`, que parsea con `TaskEnvelope.safeParse`, así que **toda** regla de bloque de A rehúsa en las dos puertas desde que el campo existe, con el path `envelope.content.…` — no se escribió un segundo validador, que habría sido otra vez la segunda autoridad. `CONTENT_REFERENCE_UNKNOWN`: cada bloque con `artifactRefId` debe resolver a una referencia de artefacto con scope `TASK` **de este** `taskId`; una de otro scope gana la misma palabra en vez de admitirse porque el id resolvió. **Verifica y no publica**: un envelope lleva una referencia y un digest y **nunca bytes**, así que no hay nada que publicar en la puerta — el productor publica lo que referencia antes de someter, y el trabajo de la puerta es rehusar contenido que apunta a nada. Que una referencia resuelva es un hecho del plano y no de la forma del payload, y por eso el contrato de A calla al respecto. `TASK_INTAKE_CODES` nueve → diez. **L-P06A-1 se enmienda en su fila**: deja de no admitir llamador y admite exactamente los dos que B cablea (el schema del envelope y el ingreso del runtime); `PATH_SCOPED_LAWS` sigue 145 | [contratos §4.1](../architecture/contracts/index.md); [ADR 0094](../../architecture/0094-the-task-envelope-carries-the-instructions-content.md) |
| 98 | **2.7.0 lo paga un campo requerido, y `LEDGER_CONTRACT_VERSION` se mueve derivada, no decidida** (P-06/B, Q3, molde decisión 85): `CONTRACT_VERSION` `2.6.0` → **`2.7.0`**, minor sobre una forma que se **emite** y no se re-lee, así que rige `AdmittedContractVersion` y el costo declarado es que una fixture de la cohorte anterior deja de parsear (consecuencia V3, N-P06-11); `SUPPORTED_CONTRACT_VERSIONS` cinco → seis sin perder ninguna, así que toda fila guardada sigue leyéndose. Los tres vectores de envelope-identity recomputados **dos veces** cada uno —por `envelopeSha256` y por `node:crypto` sobre la preimagen— y coincidentes. **`LEDGER_CONTRACT_VERSION` sube por alias**: es `= CONTRACT_VERSION` en `protocol/src/version/index.ts:211`, de modo que sus ~170 consumidores la nombran por símbolo y siguen sin una edición, y **un solo** pin literal se movió; el brief la esperaba quieta y la medición mandó. `API_CONTRACT_VERSION` `0.17.0` → **`0.18.0`** (Q3), literal independiente y por tanto decisión deliberada: el cuerpo de la ruta de ingreso lleva el envelope y el envelope cambió de forma. `MIGRATIONS` 21, `PROJECTOR_VERSION` 1 y `CONTRACTS_SCHEMA_EXPORTS` 150 quietos | [ADR 0076](../../architecture/0076-an-effect-is-looked-up-by-its-logical-key.md); [ADR 0094](../../architecture/0094-the-task-envelope-carries-the-instructions-content.md) |
| 99 | **La instrucción se compone del contenido, en el orden de la lista y con un separador, y no se compone a medias** (P-06/C, Q-C2, E9/E13, ADR 0095): `instructionFor` deja de leer `envelope.objective` y lee `envelope.content.blocks`; aporta texto **todo** bloque `text`, en el orden de la lista —porque §4.1 la llama **ordenada** y un compositor que ordenara o agrupara respondería una pregunta que el contrato ya respondió— unidos por `BLOCK_SEPARATOR = "\n\n"`, una sola regla sin variante por adaptador para que dos transportes no vean instrucciones distintas de un mismo envelope. Un bloque `text` que nombra `artifactRefId` se lee por el **verbo del plano** con `scopeKind: "TASK"` y el `scopeId` de **esta** tarea (molde verbatim del lector de objetivo de `initiative-registration`, con un lease store que rehúsa todo porque leer por referencia no le pregunta nada), y los bytes se verifican contra `contentSha256`, contra `byteLength` y contra el texto inline del bloque: cualquier desacuerdo lanza **antes** de que un byte cruce (N-P06-13) y una referencia de otro scope la rehúsa el plano con su propia palabra `SCOPE_EQUALITY_V1` (N-P06-12), nunca una segunda opinión formada aquí. En los dos casos la instrucción **no se compone en absoluto**: no hay mitad que cruzó. Las clases no-texto no aportan texto y **no se descartan**: viajan como clases al adaptador y se rehúsan ahí (fila 100), porque componer en silencio sin la parte que el llamador pidió sería mandarle al modelo algo que nadie autorizó. El guarda de credenciales corre sobre **cada** bloque resuelto antes del join y no sobre la cadena compuesta (N-P06-18): un hit nombra **cuál** bloque ofendió, como path y nunca como bytes, y rehúsa antes de que el texto ofensor se haya unido a nada. `instructionFor` queda **exportada** para que la prueba §4.3 maneje el productor real y no una copia; exportar no ensancha `L-B1C-1`, que cuenta sitios que **asignan** un `instructions:` propio, y un llamador difunde lo que el único productor devolvió | [contratos §4.1](../architecture/contracts/index.md); [ADR 0093](../../architecture/0093-an-instructions-content-is-an-ordered-list-of-discriminated-blocks.md); [ADR 0095](../../architecture/0095-the-instruction-is-resolved-on-the-private-side-of-the-adapter-boundary.md) |
| 100 | **El rechazo de modalidad vive DENTRO de la unión de entrega, viajan las clases y no los bytes, y el bound sigue al valor sin mover ninguna versión** (P-06/C, Q-C3 confirmada, E10/N-P06-15): `SessionDescriptor.delivery` gana un tercer miembro `{kind:"UNSUPPORTED"; reason:"MODALITY_UNSUPPORTED"}` y `SessionRequest` gana `modalities` —las **clases distintas** de las que se compuso la instrucción, nunca un bloque y nunca un byte—. Viajan las clases porque un `describe` **puro** no puede rechazar lo que no ve, y pasarle los bloques pondría contenido del lado público del límite del adaptador, que §4.1 `:199-201` prohíbe. Dentro de la unión y no al lado, para reusar el único punto de rehúso que ya corre **antes del spawn**; el código sigue siendo `PROTOCOL_UNSUPPORTED` por la razón del ADR 0034 —la especificidad vive en el `reason` del descriptor y acuñar un código movería un conjunto cerrado pineado sin ganancia semántica—, y el `never` obliga a **los tres** adaptadores a declararse: claude rehúsa toda clase no-texto puramente, antes de que exista proceso; codex y kimi conservan `HANDSHAKE_REQUIRED`, porque un transporte cuyo protocolo no puede tomar instrucción alguna tampoco puede tomar una de texto, y nombrar la modalidad sería una respuesta más específica a una pregunta menos fundamental. **Nombrar una modalidad no es instalarla**: nada multimodal se transporta (Q4). El bound de `ExecutionRequest.instructions` pasa de `max(4_000)` a `INSTRUCTIONS_MAX_CHARS = CONTENT_REQUEST_AGGREGATE_MAX_BYTES` —el agregado y no el producto aritmético, porque tests §9.6 regla 1 manda donde dos números se cruzan: gana la política vigente, y el agregado es el techo al que la puerta ya sometía el contenido de un pedido—; sobre el tope es **rechazo, nunca truncación**. **No es bump, y está medido**: `ExecutionRequest` no lleva `contractVersion` (es forma de puerto, no instrumento emitido), así que ninguna línea de versión se mueve ni por el bound ni por el campo nuevo. `CONTRACTS_SCHEMA_EXPORTS` 150 → 151 | [contratos §4.1, §4.3](../architecture/contracts/index.md); [tests §9.6](../quality/testing/index.md); [ADR 0034](../../architecture/0034-the-instruction-channel.md); [ADR 0095](../../architecture/0095-the-instruction-is-resolved-on-the-private-side-of-the-adapter-boundary.md) |
| 101 | **La ocurrencia de prompt gana su productor en C, y el 0080 §7 se enmienda en su fila; dos leyes nuevas y dos vacíos declarados** (P-06/C, Q-C1, E14/N-P06-17): la tabla de deudas reasignadas del ADR 0080 §7 mandó los productores de `PROMPT_OCCURRENCE_RECORDED` y `RESPONSE_OCCURRENCE_RECORDED` al packet de adopción «por la misma razón» de la fila anterior —«necesitan el puerto de ejecución real y un proveedor»—, y esa condición **se cumple en este escalón**, que es donde la instrucción cruza al adaptador: la fila queda enmendada y el productor del prompt aterriza aquí; el de la respuesta sigue reasignado, porque §4.2 y el contrato de salida son de P-07. `buildPromptOccurrenceEvent` es un **constructor puro** en el módulo de eventos del runtime, al lado de `buildEvent`: rehúsa por nombre una invocación sin revisión (una ocurrencia lleva la coordenada V2 y una invocación V1 no nombra intento al que atribuir la entrega), deriva el `transitionId` del id de la ocurrencia para que una ocurrencia reafirmada sea un replay bajo la misma clave, y parsea por `ControlPlaneEvent` para que los guardas de credencial y de transcript corran al salir. Registra digest y longitud, y `contextSha256` **null** cuando no hay contexto direccionado aparte: null y no un digest de nada, porque un digest inventado es peor que una ausencia declarada —ningún lector lo distingue de uno real—. **Apendar no es de C**: todo productor V2 de esta familia tiene su constructor aquí y su sitio de append en el packet de adopción. **`L-P06C-1`** hace mecánica a N-P06-14 —ningún bloque resuelto entra en evento, frame de stream, checkpoint, atributo de telemetría ni línea de log, **ni como digest**— por **contención** y no por prohibición de palabra (una prohibición de palabra atraparía los digests legítimos del plano de artefactos): exactamente **tres** `src/` trackeados pueden ver un bloque —el refinamiento del envelope, la puerta que valida, el único productor—, la **función** que dentro del productor tiene bytes resueltos no nombra ningún registrador (scoped a la función y no al archivo, porque la raíz de composición del daemon loguea y escribe checkpoints treinta líneas más allá), y la frase que la ley hace cumplir sigue donde el campo `instructions` la declara. **`L-P06C-2`** pinea, bidireccional y sobre el molde de `CONTRACTS_SCHEMA_EXPORTS`, que el record del productor y la gramática del ledger son los mismos trece nombres y que ninguno lleva `identity` —esa columna es el `emittedBy` del evento—, porque el `payload` del contrato de eventos es un record de unknowns para todo tipo y lo que mantiene fuera una clave ajena es este productor. `PATH_SCOPED_LAWS` 145 → **146** (L-P06C-2 no es path-scoped y no agrega fila); `L-B1C-1` se enmienda **en su fila** (asertaba `envelope.objective`, ahora `envelope.content.blocks`) y `L-P06A-1` por segunda vez, para admitir la frontera de ejecución que nombra el concepto por su **vocabulario** y sus **bounds** y no construye lista alguna. **Dos vacíos declarados y no tapados**: `objective` **no** se retira —retirarlo mueve `CONTRACT_VERSION` y C no mueve versión—, sigue atado al primer bloque de texto y su retiro es deuda del próximo bump de envelope; y `ApiStreamRequest` **no lleva instrucción** (lleva modelo, tarea, intento e identidad), así que «un hijo que devuelve lo que recibió» se prueba entera en la pata **CLI** —productor real, puerto real, adaptador real, hijo real que escribe a un archivo lateral los bytes que recibió— y la mitad de la pata API queda como deuda del transporte API, medida y registrada; no es defecto vivo, porque el documento de capacidad embarcado no admite transporte API y el drill lo afirma contra el documento real | [contratos §4.1, §4.3](../architecture/contracts/index.md); [ejecución §8](../architecture/database/execution/index.md); [ADR 0077](../../architecture/0077-a-prompt-occurrence-is-a-use-never-a-blob.md); [ADR 0080](../../architecture/0080-the-producer-speaks-the-v2-coordinate.md); [ADR 0095](../../architecture/0095-the-instruction-is-resolved-on-the-private-side-of-the-adapter-boundary.md) |
| 102 | **El registro de una ocurrencia de prompt se construye campo por campo, y la gramática del ledger es su única autoridad** (P-06/CORR, F1 de la auditoría Codex de la pausa 2026-09-15): `buildPromptOccurrenceEvent` deja de hacer `{ ...occurrence }` —un spread copia toda clave propia de un valor que TypeScript deja pasar más ancho que su tipo, y la puerta rechaza por nombre la clave extra después de enviada la instrucción— y escribe un literal explícito de los trece nombres, tipado como `PromptOccurrenceRecord`. No se agrega schema Zod en contracts ni en runtime: sería una segunda autoridad sobre el mismo conjunto (clase de la decisión 45). El barrel de `@acp/ledger` exporta `PROMPT_OCCURRENCE_RECORD_KEYS` (opción B, precedente `TASK_INTAKE_PAYLOAD_KEYS`); el lector sigue interno, así que la igualdad de claves contra esa gramática es la prueba de gramática de la suite del productor. L-P06C-2 se enmienda en su fila: lee la declaración en la hoja del concepto, exige que el literal construido nombre exactamente la gramática y que el payload lo lleve, y falla ante un spread en el builder | [ADR 0096](../../architecture/0096-the-api-and-local-legs-carry-the-composed-instruction.md); [ADR 0095](../../architecture/0095-the-instruction-is-resolved-on-the-private-side-of-the-adapter-boundary.md) |
| 103 | **Las patas API y local llevan la instrucción compuesta, y rehúsan antes del cliente lo que no pueden llevar** (P-06/CORR, F2): `ApiStreamRequest` y `LocalChatRequest` ganan `readonly instructions: string` en su propio módulo, y `API_CLIENT_SHAPE`/`LOCAL_CLIENT_SHAPE` pasan de 4 a 5 miembros a propósito. Tras admitir la ruta (el rechazo de reattach sigue siendo la primera sentencia de cada pata, L-B4A-3 intacta) y antes de armar el pedido, un helper privado rehúsa toda clase distinta de `text` y corre sobre la instrucción el mismo `findCredentialViolations` que `startSession`; ambas respuestas son `TRANSPORT_UNAVAILABLE`, el miembro con que la pata CLI contesta las mismas dos condiciones, con `at` = `request.modalities` o `request.instructions`, sin nombre de rechazo nuevo y con cero llamadas al cliente. La instrucción viaja como `instructions: asked.instructions`, pass-through de L-B1C-1. La afirmación de ADR 0095 «not a live defect» queda corregida por errata: el daemon ejecuta la ruta de su configuración sin re-resolverla, y R6 llega a checkpoint por `API_KEY` | [ADR 0096](../../architecture/0096-the-api-and-local-legs-carry-the-composed-instruction.md); [contratos §4.1](../architecture/contracts/index.md) |
| 104 | **§4.3 se parte en su frontera, sin reubicar nada** (P-06/CORR): P-06 prueba el transporte y el rechazo previo a la llamada —la suite del puerto con clientes sintéticos que registran, para API y local; el drill del daemon invertido, cuyo pedido API lleva exactamente la instrucción compuesta; y un eco API sintético por `startDaemon` que devuelve lo que recibió, igual a lo que compuso `instructionFor`, más un negativo donde una clase no textual se rehúsa antes del cliente con cero llamadas—. El camino puerta de ingreso → envelope registrado leído por referencia → hijo es de **P-15**, como ya lo asignan la fila de P-14, la de P-15 y «Not in this record» de ADR 0095; no es una reubicación nueva. Clientes HTTP/locales reales y el smoke real también son de P-15. Ninguna prueba presenta el fin de un stream como resultado de una operación: el contrato de salida es de P-07. B5 no cierra en P-06; cierra en M2 con P-15 | [ADR 0096](../../architecture/0096-the-api-and-local-legs-carry-the-composed-instruction.md); [contratos §4.3](../architecture/contracts/index.md) |
| 105 | **Los tres tipos nuevos de P-06/C van a su hoja, y el diferimiento v4 de los dos de runtime a P-37 queda superseded** (P-06/CORR, F3; ley del owner de [arquitectura §7](../architecture/index.md)): `PromptOccurrenceRecord` y `BuildPromptOccurrenceInput` pasan a `packages/domains/runtime/src/core/events/types/index.ts` (hoja nueva) y `ComposedInstruction` a la hoja existente `packages/entrypoints/daemon/src/composition/types/index.ts`; cada módulo los importa con `import type` y los re-exporta con `export type`, así que ningún importador cambia y ningún barrel se mueve. La adjudicación v4 de P-06/C había anotado los dos de runtime como deuda de P-37; un antecedente contrario a una ley del owner es deuda, no permiso, y se paga aquí. `BuildEventInput` queda inline: es deuda histórica de P-37 y no es parte de F3 | [ADR 0096](../../architecture/0096-the-api-and-local-legs-carry-the-composed-instruction.md); [decisión 90](#2-adjudicaciones-técnicas-de-esta-ronda) |
| 106 | **El contrato de resultado v1 es una sola forma: estado de dos palabras, bloques del contrato de contenido y referencia de uso igual al efecto** (P-07/A; adjudicación v2 C3, C4, C5; preauditoría Fable D3, D6; respuestas del DT Q-A1..Q-A5): `ResultContractSchema` es `strictObject` con cinco claves requeridas —`resultContractVersion: 1`, `effectId`, `status`, `blocks`, `usageReference`—. `status` ∈ {`SUCCEEDED`, `FAILED`} (D6); `CANCELLED` y `OUTCOME_UNKNOWN` son del efecto, no del resultado. `blocks` es `z.array(ContentBlockSchema)` (D3: no hay otro formato), ordenada, con las cinco clases admitidas (Q-A3) y toda regla de bloque heredada bajo `blocks.<i>`. `SUCCEEDED` exige al menos un bloque (`RESULT_BLOCKS_REQUIRED`, Q-A2); `FAILED` puede no traer ninguno. `usageReference === effectId` o `USAGE_REFERENCE_MISMATCH` (C5, economía §2.1). Topes propios que aliasan unidad por unidad (Q-A5): `RESULT_BLOCK_LIST_MAX = CONTENT_BLOCK_LIST_MAX` (cuenta) y `RESULT_AGGREGATE_MAX_BYTES = CONTENT_REQUEST_AGGREGATE_MAX_BYTES` (bytes). Representación C4: texto en bloques de ≤ 4 000 caracteres hasta 100, más allá un `document` por referencia, nunca truncar; elegir entre ambos es regla del ensamblador de D, no invariante del schema, así que texto y documento juntos son lícitos (Q-A1). Sin refinamiento `isError`: la regla operativa es (iii), el flag terminal del proveedor; (i) queda inerte y declarada, sin productor de bloques `tool_result` de salida en P-07 (C3). Tipos en la hoja `result/types/index.ts` (Q-A4). Sin bump: el documento lleva su propia versión y nada lo persiste todavía; B mueve a 2.8.0 | [ADR 0097](../../architecture/0097-a-result-is-an-ordered-list-of-output-blocks-under-its-effect.md); [contratos §4.2](../architecture/contracts/index.md) |
| 107 | **El resultado nace inerte, y L-P06A-1 lo admite como consumidor de la forma única** (P-07/A): **L-P07A-1** (nueva, con scope de ruta; `PATH_SCOPED_LAWS` 146 → 147) rechaza toda fuente de producción fuera del concepto `result` y de los dos barrels de contracts que importe su módulo o nombre `ResultContractSchema`, `ResultContract`, `RESULT_CONTRACT_VERSION`, `RESULT_STATUSES` o `RESULT_REFUSALS`; D la enmienda en su fila cuando el ensamblador lo consuma. **L-P06A-1** se enmienda en su fila para admitir `schemas/result/index.ts`: consume la forma única y no construye ninguna lista de contenido; sin fila nueva. **L-P06C-1** queda intacta por construcción: el código del módulo no nombra la referencia de un bloque ni la lista de contenido de una instrucción. `CONTRACTS_SCHEMA_EXPORTS` 151 → 160 | [ADR 0097](../../architecture/0097-a-result-is-an-ordered-list-of-output-blocks-under-its-effect.md); [decisión 94](#2-adjudicaciones-técnicas-de-esta-ronda) |
| 108 | **El efecto registra su resultado por referencia, con su desenlace, y `SUCCEEDED` lo exige** (P-07/B; adjudicación v2 C1, C2, D1, D2, D6; respuestas del DT Q-B1, Q-B2, Q-B3): `DISPATCH_OUTCOME_RECORDED` puede llevar en `payload.outcome` el par `resultArtifactReferenceId` + `resultSha256` —la referencia registrada del artefacto `RESPONSE` de la tarea y su `content_sha256` conservado—, atómico con el desenlace. La migración 22 agrega a `effect_read_model` `outcome_contract_version`, `result_artifact_reference_id` y `result_sha256` (nullable, sin reescribir filas): CHECK para la ley de fila independiente de versión (par ambos NULL o ambos presentes, forma SHA, resultado sólo en `SUCCEEDED`/`FAILED`, versión ⇒ desenlace) y dos triggers, `BEFORE INSERT` y `BEFORE UPDATE OF` las cuatro columnas (Q-B1: el rebuild inserta), para desenlace ⇒ versión y la cohorte —lista cerrada `2.2.0`…`2.7.0` sin resultado; `SUCCEEDED` posterior con resultado—. La mitad desenlace ⇒ versión no puede ser CHECK añadido sobre filas existentes (Q-B2); un `afterSql` la completa desde el evento vía `dispatchOutcomeRecord` (molde de la 18), escribiendo también el par para que un ledger rebobinado pasado 22 se reaplique a lo que dicen sus eventos, con control del control. El lector rechaza present-invalid, medio par, par sin desenlace, par en `CANCELLED`/`OUTCOME_UNKNOWN`, par en la cohorte anterior y `SUCCEEDED` posterior sin par; `FAILED` admite ambos. La puerta verifica existencia, clase `RESPONSE`, scope de esta tarea y digest (datos §11 paso 7); la base sólo presencia. `effectOutcomeArrival` es la única comparación de replay/conflicto sobre estado y par para puerta y fold; una fila de la cohorte anterior sin par frente a un par se rechaza (C2, apartándose declaradamente del «cuenta sólo donde la fila lo tiene» del ADR 0084 Cinco). Bump `2.7.0` → `2.8.0` por cohorte (ADR 0084 Dos), `SUPPORTED_CONTRACT_VERSIONS` seis → siete, tres vectores de envelope-identity recalculados dos veces, `LEDGER_CONTRACT_VERSION` por alias, `API_CONTRACT_VERSION` quieto. Q-B3: el drill de reutilización de runtime pasa a `FAILED` (la ley no tiene excepción para `FAILED` y ese arnés no tiene fixtures de artefactos) | [ADR 0098](../../architecture/0098-an-effect-records-its-result-by-reference-with-its-outcome.md); [ejecución §6](../architecture/database/execution/index.md); [contratos §4.2](../architecture/contracts/index.md) |
| 109 | **El ledger lee `RESULT_STATUSES` del contrato, nunca una copia, y L-P07A-1 lo admite para ese nombre solo** (P-07/B, regla del DT): `ledger/src/projection/index.ts` importa `RESULT_STATUSES` de `@acp/contracts` —arista persistencia → kernel existente, sin ciclo ni dependencia nueva— y la gramática del desenlace lo usa para rechazar un par fuera de los dos estados. L-P07A-1 se enmienda en su fila: el archivo entra en `RESULT_CONTRACT_SITES` y, en el mismo bloque, un chequeo aparte falla si su código nombra cualquier otro nombre del contrato de resultado o importa la ruta del módulo, para que la admisión no se ensanche en silencio; `PATH_SCOPED_LAWS` sigue en 147. El SQL de la migración 22 no puede importar: su `('SUCCEEDED', 'FAILED')` es la única reenunciación, forzada por la inmutabilidad, y la suite de migraciones la sostiene igual a `RESULT_STATUSES` | [ADR 0098](../../architecture/0098-an-effect-records-its-result-by-reference-with-its-outcome.md); [decisión 107](#2-adjudicaciones-técnicas-de-esta-ronda) |
| 110 | **Transporte, proceso y operación son tres hechos, en orden fijo y nunca fundidos** (P-07/C; adjudicación v2 C6): `ExecutionEvent` gana `processExited { exitCode, signal }` —exactamente uno no nulo; código 0..255, señal `^SIG[A-Z0-9]+$` de hasta 16— y `operationResult { status }` con `status` de `RESULT_STATUSES` (una sola autoridad, Q-C8). Ninguno es terminal; cuando están, van en ese orden antes del único terminal y `completed` sigue siendo sólo éxito de transporte. Ausente es NO_OBSERVABLE, nunca exit 0: las patas API/local (no poseen proceso), el camino de throw del puerto y un kill cuya gracia vence. Una sesión FAILED informa el `SIGKILL` de nuestra escalera, o el estado propio del hijo si ya había salido, y no informa veredicto. Codex y Kimi: veredicto y salida no observables por el puerto | [ADR 0099](../../architecture/0099-a-transport-a-process-and-an-operation-are-three-facts.md); [contratos §4.2](../architecture/contracts/index.md) |
| 111 | **Se retira `text` y la salida tiene un solo hogar privado** (P-07/C; C8; Q-C2, Q-C4, Q-C5): `start(route, request, sink?)` recibe `ExecutionOutputSink` como tercer argumento opcional (`ExecutionRequest` es estricto y no lo aloja); el sink recibe sólo texto de salida, delta a delta, y lo que recibe no entra en ningún evento. Claude emite una señal privada `output` por bloque `text` de cada `assistant`, en orden, excluyendo `thinking` y registros con `is_api_error_message: true`; `result.result` no se envía. La sesión la entrega al sink en una función que no nombra registradores; sin sink se descarta (camino heredado, C9). API/local enrutan su `text` igual, y sus uniones de chunk ganan `operationResult`, retenido por el puerto una sola vez y emitido en orden (sin productor en P-07); su mapeo chunk→evento es un switch exhaustivo, así que un cliente que emite `processExited`, `completed` o `error` hace fallar el stream. Un sink que lanza falla la sesión como `MALFORMED_EVENT`. Un reattach que pide sink se rechaza dentro de la conjunción existente (L-B4A-3 sigue en 3). **L-P07C-1** (nueva, `PATH_SCOPED_LAWS` 147 → 148): siete archivos `src` más los dos barrels de contracts nombran la señal o el tipo del sink; el enrutador de la sesión no nombra registradores; la oración del límite se queda. L-P07A-1 se enmienda en su fila para admitir `execution-boundary` por `RESULT_STATUSES` solo, en una forma de import. `CONTRACTS_SCHEMA_EXPORTS` 160 → 161 | [ADR 0099](../../architecture/0099-a-transport-a-process-and-an-operation-are-three-facts.md) |
| 112 | **Claude lee `is_error` sobre la evidencia de dos muestras capturadas, y nada más** (P-07/C; C7; Q-C1 revisada por la muestra 2, Q-C3, Q-C6, Q-C7): `is_error: true` → `FAILED` (muestra 1, salida 1), `false` → `SUCCEEDED` (muestra 2, salida 0), ausente → sin veredicto, cualquier otro valor → `MALFORMED_EVENT` (ADR 0079); `subtype` sigue siendo un token de estado abierto y no un veredicto (`"success"` apareció en ambas). Un segundo veredicto en una sesión la falla con `MALFORMED_EVENT`. `system/commands_changed` y `rate_limit_event` son registros reconocidos sin señal —este último sólo con `status: "allowed"`, el único observado; cualquier otro falla cerrado— y `rate_limit_event` nunca se mapea a presión de cuota. Un `content` que no es arreglo o un bloque que no es objeto se rechaza (`MALFORMED_EVENT`). Los pares cruzados (`true` con salida 0, `false` con salida ≠ 0) sólo se prueban sintéticos. Fixture: ambas muestras saneadas como literales TS, iguales byte a byte a los archivos (sha256 `e8c72c6f…c502` y `01132951…d312`). **Hallazgo nombrado, no resuelto en C:** los registros `assistant` de un mismo mensaje repiten `message.usage`, así que el `step` por registro cuenta dos veces una cifra parcial (4 + 4 frente a 44); es de P-15 (normalización real de clases y retiro de L-P32C-1) con la muestra 2 como evidencia, y D no trata esos conteos como liquidación. Para P-15 quedan también el allowlist de entorno (auth resuelto con `USER`) y `--verbose` (no observado) | [ADR 0099](../../architecture/0099-a-transport-a-process-and-an-operation-are-three-facts.md) |
| 113 | **El desenlace de la operación se decide una sola vez, sobre los tres hechos de C** (P-07/D; adjudicación v2 C8, C10; Q-D1, Q-D6; respuesta del DT sobre `OUTPUT_UNREADABLE`): el concepto `runtime/src/operation-result/` lee el trail por `kind` y nada más —`state.toState` nunca es veredicto— y rechaza un segundo terminal, salida o veredicto en vez de sobrescribirlo. Sólo se decide un terminal `completed`: operación `FAILED` → `FAILED OPERATION_FAILED`; `SUCCEEDED` con salida 0 o no observable → `SUCCEEDED OPERATION_SUCCEEDED`; `SUCCEEDED` con otro código o una señal → `FAILED PROCESS_ABNORMAL`; operación no observable → `FAILED OPERATION_NOT_OBSERVED`. Los pares cruzados son `FAILED`. El vocabulario de razones es cerrado y ordenado: `NO_OUTPUT`, `OPERATION_FAILED`, `OPERATION_NOT_OBSERVED`, `OPERATION_SUCCEEDED`, `OUTPUT_OVER_PROFILE`, `OUTPUT_REFUSED`, `OUTPUT_UNREADABLE` (palabra propia: una falla del sink no es un rechazo del guard) y `PROCESS_ABNORMAL`. Un terminal `error` no llega al registrador | [ADR 0100](../../architecture/0100-an-effect-answers-with-a-published-result-and-its-response-occurrence.md); [contratos §4.2](../architecture/contracts/index.md) |
| 114 | **El ensamblador sigue la regla C4 y el publicador le pregunta al ledger antes de mover un byte** (P-07/D; C9, D7–D10; Q-D2, Q-D4, Q-D9; respuestas del DT, ronda 2(a)): la salida se corta en trozos de hasta 4 000 unidades UTF-16 sin partir un par sustituto; hasta 100 trozos son bloques `text` (`output-001`…, nueve claves, `artifactRefId`/`toolCallId`/`effectId` nulos); más de 100 son un único documento `text/markdown` publicado primero y nombrado por referencia desde un bloque `document`; nunca ambos. Más de 8 MiB → `FAILED OUTPUT_OVER_PROFILE`, sin truncar; el guard de credenciales lee la salida entera antes de cortarla (una credencial partida por un borde de trozo también se atrapa) y rechaza como `OUTPUT_REFUSED`, sin publicar ni redactar (D10); `SUCCEEDED` sin salida → `FAILED NO_OUTPUT`; `FAILED` con salida publica su documento con estado `FAILED` (Q-D9). El publicador lee el efecto (`EFFECT_UNKNOWN` si no existe), arma la llegada candidata —estado, digest recién calculado y la referencia guardada si la hay— y **llama** a `effectOutcomeArrival`, que el barrel del ledger ahora exporta junto con `EffectOutcomeArrival` y `RESPONSE_OCCURRENCE_RECORD_KEYS` (esto supera las oraciones de «ningún nombre nuevo en el barrel» de ADR 0084 y ADR 0098 para esos tres nombres): `refused` → `CONFLICT` con el `path` y el `message` del ledger, antes de publicar; `replay` → el par guardado, sin publicar; `write` → publica el desborde y luego el documento. El artefacto es `RESPONSE` `INTERNAL`, alcance `TASK`, `SCOPE_EQUALITY_V1`, `PERMANENT` sin vencimiento, `application/json`, con claves `<effectId>/result\|output/<sha256>/intended\|succeeded`; una publicación interrumpida retoma la intención registrada. Sin documento no hay par: referencia, digest y largo son nulos juntos. **Desvío declarado (N-P07D-19′):** la puerta no puede comparar la llegada literal (referencia guardada con el digest nuevo) porque antes verifica que la referencia contenga esos bytes; el test le pregunta por la llegada más cercana que sí compara (los bytes nuevos bajo su propia referencia) y verifica que rechaza con la misma comparación, en el primer miembro que difiere. La procedencia la prueba el espía (N-P07D-19″). Los efectos de ejecución tienen un colector privado cuyo sink nunca lanza (`try … catch`; más de 8 MiB → `OVER_PROFILE`, delta que no es string o falla → `UNREADABLE`); con registrador `start` recibe tres argumentos, sin registrador dos, y el marcador queda idéntico (C9). El registrador se llama una vez, sólo en `completed`, antes de la compuerta y del marcador | [ADR 0100](../../architecture/0100-an-effect-answers-with-a-published-result-and-its-response-occurrence.md); [contratos §4.2](../architecture/contracts/index.md) |
| 115 | **La ocurrencia de respuesta se cierra por construcción, y las leyes del escalón** (P-07/D; Q-D5, Q-D7, Q-D8; adición del DT F1): `buildResponseOccurrenceEvent` rechaza una invocación V1 por nombre y arma su registro como un literal tipado de las cinco claves del ledger (`occurrenceId`, `promptOccurrenceId`, `responseSha256` = `content_sha256` del `RESPONSE` publicado, `responseBytes`, `redactionVerdict`); un valor más ancho que el tipo produce exactamente esas cinco, y un campo presente e inválido viaja tal cual para que lo juzgue la gramática de la puerta. No hay constructor de desenlace en D: los drills agregan `DISPATCH_OUTCOME_RECORDED` por la puerta con el par que devolvió el publicador. El concepto se llama `operation-result` y no `result`, para que su propia ruta no coincida con el patrón de L-P07A-1. Leyes: **L-P07A-1** admite el concepto y su hoja de tipos como sitios plenos; **L-P06C-1** admite `operation-result/index.ts` como el único productor de bloques de salida (escribe `artifactRefId`, nunca `content.blocks`); **L-P07C-1** suma `execution-effects` a los sitios (ocho), prohíbe registradores en `collectOutput`, `assembleResult` y `publishResult`, y con **F1** lee el árbol sintáctico de todo `src` rastreado y admite un `.start(` de tres argumentos sólo en `execution-effects` (y falla si esa llamada desaparece mientras exista un registrador); F1 mira la forma de la llamada —callee `.start` o `["start"]`, en `.ts` y `.tsx`, con tres o más argumentos o cualquier spread— y un callee con alias (`.bind`, un `start` desestructurado) queda como límite declarado; la contención de L-P07C-1 nombra también a `textBlock`, `documentOf`, `publishDocument` y `publicationRequest`; **L-P07D-1** (nueva, fijación de conjunto de nombres, sin fila) iguala declaración, literal construido y gramática en ambos sentidos, sin spread y sin `identity`, `dispatchAttemptId`, `routeSegmentId` ni `accountId`; **L-P07D-2** (nueva, con scope de ruta; `PATH_SCOPED_LAWS` 148 → 149) exige una sola llamada al registrador, después del rechazo y antes de la compuerta y del marcador, detrás de un sink envuelto en un único `try … catch` sin `throw`. **L-F4E-1** lee su ancla `await execute(input` como prefijo. El barrel del runtime no exporta nada nuevo (`RUNTIME_PUBLIC_EXPORTS` sigue en 285); `CONTRACT_VERSION` 2.8.0, `MIGRATIONS` 22 y `CONTRACTS_SCHEMA_EXPORTS` 161 no se mueven. Límites, de P-15: sin append desde el daemon, sin registrador en producción (C9), sin verbo de lectura, sin acoplar el terminal de la tarea al desenlace (C10), el doble conteo de uso de C (Q-C7); B15 sigue abierto | [ADR 0100](../../architecture/0100-an-effect-answers-with-a-published-result-and-its-response-occurrence.md); [contratos §4.2](../architecture/contracts/index.md) |
| 116 | **`USER` entra al entorno de Claude y de ningún otro proveedor** (P-15/A; ND-8): `config-root` gana `PROVIDER_EXTRA_ENV` (`claude: ["USER"]`, `kimi: []`, `codex: []`); `allowedEnvKeys` es base + variable de configuración + extras, ordenado, y `buildEnv` copia los extras del padre clave por clave, sin spread. Un extra que el padre no tiene queda ausente —ni cadena vacía ni inventado—; un valor presente se copia tal cual, como las claves base. Evidencia: bajo `env -i HOME PATH CLAUDE_CONFIG_DIR`, `claude auth status` da `loggedIn: false`; agregar sólo `USER` lo da vuelta (dos veces), un `USER` equivocado no, y ninguna de las otras 51 variables tampoco. `USER` es un nombre de login, no un secreto. `PROVIDERS_ENV_ALLOWLIST.claude` pasa de 4 a 5, enmendado en su fila, y la igualdad lee también `PROVIDER_EXTRA_ENV`, proveedor por proveedor. **No probado:** que el entorno del daemon bajo launchd tenga `USER`; si falta, el hijo informa «Not logged in» y es una operación `FAILED` clasificada | [ADR 0101](../../architecture/0101-the-claude-adapter-speaks-the-observed-cli.md) |
| 117 | **El argv de Claude es el que las capturas observaron, flag por flag** (P-15/A; ND-9; Q-A3): `-p --output-format stream-json --verbose --model <alias> --session-id <v5> --no-session-persistence --strict-mcp-config --mcp-config {"mcpServers":{}}`. `--verbose` porque el binario exige `requires --verbose` para `stream-json` bajo `-p` y ambas capturas lo pasaron; `--no-session-persistence` para que no quede transcript del proveedor bajo la raíz de configuración; la configuración MCP vacía y estricta para que ningún servidor MCP de la cuenta llegue a un worker. Un revisor suma `--tools Glob,Grep,Read,WebFetch,WebSearch` a `--permission-mode plan --restricted`, y `descriptorEnablesWrites` sigue dando falso. Quedan fuera del adaptador `--tools ""`, `--max-turns`, `--safe-mode` (desactivaría `AGENTS.md`/`CLAUDE.md`) y `--max-budget-usd` (no es un tope duro y no hay valor admitido). **No probado** (lo cubre el smoke S1, sin captura 3): el argv exacto, `--session-id` con persistencia apagada y su eco en `init.session_id`, la lista `--tools` del revisor frente a `--restricted`, sesiones de varios turnos o con herramientas, otro modelo u otra versión de la CLI, y que no se escriba nada bajo la raíz de configuración | [ADR 0101](../../architecture/0101-the-claude-adapter-speaks-the-observed-cli.md) |
| 118 | **Un nombre de sesión por intento, y `--resume` sólo con ese mismo nombre** (P-15/A; C8; Q-A1, Q-A2): `claudeSessionId(taskId, attempt)` es un UUID v5 RFC 4122 sobre `taskId + "/" + attempt` bajo `CLAUDE_SESSION_UUID_NAMESPACE`, propio y distinto del espacio de coordenadas del runtime, fijado por vectores calculados fuera del paquete; un `taskId` que no es cadena no vacía o un `attempt` que no es entero seguro positivo se rechaza como `PROTOCOL_UNSUPPORTED`, nunca se coacciona. La hoja `providers/src/session-name/` es el único sitio `node:crypto` de providers: **L-P15A-1** (nueva, con scope de ruta; `PATH_SCOPED_LAWS` 149 → 150) exige que importe `node:crypto`, llame `createHash("sha1")` una vez y no nombre azar, reloj ni entorno, y rechaza `node:crypto` y todo nombre de hash o acuñado en cualquier otra fuente de providers. Un `resumeSessionId` igual al nombre del intento da `--resume <id>` sin `--session-id`; cualquier otro valor (el de otro intento, el `taskId`, vacío, de otro tipo) hace que `describe` lance `AdapterError("PROTOCOL_UNSUPPORTED")` antes de que exista un argv, así que no hay spawn; por el puerto aparecería como `TRANSPORT_UNAVAILABLE` en `startSession/PROTOCOL_UNSUPPORTED`, sin palabra nueva. `REATTACH_UNAVAILABLE` sigue siendo la palabra del puerto para todo reattach que el puerto ve; la rama del adaptador es hoy inalcanzable desde el puerto (`resumeSessionId` siempre nulo) y la cubre un negativo a nivel de adaptador. Con la persistencia apagada, un reattach tras la muerte del daemon queda no probado y rechazado. `PROVIDERS_PUBLIC_EXPORTS` 88 → 91 (`claudeSessionId`, `CLAUDE_SESSION_UUID_NAMESPACE`, `PROVIDER_EXTRA_ENV`) | [ADR 0101](../../architecture/0101-the-claude-adapter-speaks-the-observed-cli.md) |
| 119 | **Los productores excepcionales hablan la coordenada V2, desde un solo helper** (P-15/B; adjudicación v2 C2 en su alcance; Q-B1): `payloadCoordinate(invocation)` en `core/coordinates` devuelve nada para una invocación V1 —ninguna clave, no claves `undefined`— y `{revisionNumber, attemptNumber}` de la **revisión** para una V2, nunca del `attempt` plano (ADR 0090 Three), campo por campo. `buildEvent` arma su base con él, byte a byte igual (vectores V2 de ambos planes levantados del código previo a B). Presión, el reproductor de switch y su aterrizaje, los dos asentamientos y el recibo de herramienta lo esparcen tras sus campos, y cada uno rechaza por nombre una invocación V2 cuyo intento no se abrió, antes de construir, sondear o agregar nada (`assertAttemptOpened`, estrechado a una lectura del ledger y la invocación, exportado a esos módulos y no al barrel). La operación `runToolCall` hace la misma comprobación antes de su lectura de replay, y por lo tanto antes del claim y del puerto: un test «never emits» mostró que, sin ella, un intento V2 nunca abierto llamaba al puerto una vez y recién después el recibo lo rechazaba, dejando una llamada ejecutada sin fila (ampliación del DT a una ruta, `tool-call/index.ts`; probado con `port.calls() === 0` y sin fila de claim, y con el control positivo abierto y el camino V1 sin cambios). Un candidato de plan que nombra `revisionNumber` o `attemptNumber`, con cualquier valor y en walk V1 o V2, se rechaza antes de todo append. `restateInvocation` lee una tarea que abre con su apertura: la revisión de su payload (ausente, `null`, vacía o de otro tipo → `DISCOVERY_UNREADABLE` en `task.firstSequence`, nunca leída como «sin apertura»); una apertura que nombra una invocación que no es la de esta coordenada es igualmente ilegible —el id se deriva sólo de la tarea y del intento plano (ADR 0080 deja la revisión fuera), así que no certifica nada del digest de la submission: es un campo presente e inválido, no un `SUBMISSION_DIGEST_MISMATCH` (Q-B1 corregida tras la postauditoría; sin palabra nueva)—; y el descubrimiento por su clave V2 (ausente → `DISCOVERY_UNREADABLE` en `attempt.discovery`), cuyo chequeo de digest sigue siendo el que responde por la submission. `LifecycleRecoveryPort` gana `getEventByIdempotencyKey`. Una tarea que empieza por el intake sigue rechazada hasta P-15/D, así que una tarea admitida por el intake de P-14/C no puede cancelarse ni adjuntarse por la puerta de ciclo de vida hasta entonces. V1 no se mueve: cada productor fija el sha-256 de su evento V1 levantado corriendo el código de `313512d`; la matriz de presencia (72 celdas) deja entrar exactamente «sin par en V1» y «par válido completo en V2» | [ADR 0102](../../architecture/0102-the-exceptional-producers-speak-the-v2-coordinate.md); [ADR 0080](../../architecture/0080-the-producer-speaks-the-v2-coordinate.md) |
| 120 | **Bajo V2 no hay `TOKEN_USAGE_RECORDED`, y la cuota no ve el gasto V2 hasta P-19** (P-15/B; adjudicación v2 C1, ADR 0090): `recordTokenObservation` no se adopta y queda intacto; con una invocación que lleva revisión, el contrato lo sigue rechazando, y un test de caracterización lo fija con delta cero. El gasto V2 se registra sólo como `USAGE_STREAM_DECLARED` / `USAGE_OBSERVATION_RECORDED`, que cablea P-15/D. Hasta P-19, `readAccountUsage`, el fold de cuota de accounts y los rollups **no ven gasto de un walk V2**: dicho explícitamente, no descubierto. El límite de un smoke es el tope escrito del owner más la presión del proveedor, no el fold de cuota | [ADR 0102](../../architecture/0102-the-exceptional-producers-speak-the-v2-coordinate.md); [ADR 0080](../../architecture/0080-the-producer-speaks-the-v2-coordinate.md) |
| 121 | **L-B7R-3 se enmienda en su fila: el asentamiento lleva digest, razón y coordenada, y nada más** (P-15/B; Q-B2): el payload de `TASK_FAILED` es `{ submissionDigest, reason, ...payloadCoordinate(invocation) }`, y el módulo tiene exactamente un `payload:`; la prohibición de mensajes (`error.message`, `String(error)`, `.stack`) no cambia. Mismo `requireScope`: `PATH_SCOPED_LAWS` sigue en 150. No se agrega una ley estructural que escanee palabras sobre `ControlPlaneEvent.parse` (Q-B2): la guardia son los tests V2 por productor. `CONTRACT_VERSION` 2.8.0, `MIGRATIONS` 22, `CONTRACTS_SCHEMA_EXPORTS` 161, `RUNTIME_PUBLIC_EXPORTS` 285 y `LIFECYCLE_RECOVERY_REFUSALS` no se mueven | [ADR 0102](../../architecture/0102-the-exceptional-producers-speak-the-v2-coordinate.md); [ADR 0080](../../architecture/0080-the-producer-speaks-the-v2-coordinate.md) |
| 122 | **La entrega fija su versión de catálogo de precios, por cohorte** (P-15/C; adjudicación v2 C3 (v)): un `DISPATCH_INTENDED` lleva `catalogDocumentId` y `catalogVersion` en `payload.dispatch`; `dispatch_attempt_read_model` gana `dispatch_contract_version`, `catalog_document_id` y `catalog_version` (migración 23, aditiva, con `CHECK` de no vacío, versión ≥ 1 y par ambos-o-ninguno, dos triggers de cohorte cuya primera sentencia rechaza una fila sin versión, y backfill de la versión portadora desde la propia intención). La cohorte anterior es la lista cerrada `PRE_CATALOG_PIN_CONTRACT_VERSIONS` (2.2.0–2.8.0), igual en el fold, la puerta y los triggers: ahí el pin es nulo; desde 2.9.0 es obligatorio. `dispatchPinReading` lee cada clave presente-inválido (`null`, `""`, `0`, `"1"`, fracción → rechazo nombrando el campo, nunca ausente), rechaza medio par, pin en la cohorte anterior y ausencia en la posterior; puerta, escritura y fold preguntan primero y rechazan con sus palabras. El pin integra el nacimiento: otro pin para la misma entrega es el conflicto «intended once». Matriz SQL cruda 11 × 3 × 3 sobre INSERT y UPDATE contra un oráculo independiente. `CONTRACT_VERSION` 2.8.0 → 2.9.0 (ocho soportadas), `MIGRATIONS` 22 → 23, `tr_` 11 → 13 | [ADR 0103](../../architecture/0103-a-delivery-pins-the-price-catalog-version-it-will-be-valued-against.md); [ejecución §7](../architecture/database/execution/index.md) |
| 123 | **Vigente y cubre, antes del gasto; un modelo sin versión resuelta no se despacha** (P-15/C; C3 (ii)–(iv); Q-C1): para una entrega de 2.9.0 en adelante la puerta pregunta al registro, antes de que exista la entrega y por lo tanto antes de llamar a ningún proveedor, (a) que el pin nombre una versión publicada de un `PRICE_TABLE`; (b) que sea la vigente en el `occurredAt` canónico del evento —la de mayor `effective_from` ≤ ese instante (`selectVigentCatalogVersion`); ninguna vigente o un empate en ese instante se rechaza, nunca se elige— y (c) que cubra el segmento (`pinCovers`: un intervalo de esa versión con el proveedor, el `model_version_id` y el `transport_kind` del segmento, ventana semiabierta). El instante es el canónico del walk, no el de la llamada real: el `occurredAt` que lleva cada evento, que es el instante de submission de la invocación (para una tarea ingresada por el intake, el `occurredAt` del intake), derivado y nunca leído de un reloj. Ese instante debe estar en la forma canónica del contrato (ISO-8601 con milisegundos, UTC, terminado en `Z`; obligación heredada de ADR 0092), porque la selección compara instantes como texto y el `Timestamp` del contrato admite offsets y otras precisiones: una entrega de 2.9.0 en adelante con un `occurredAt` no canónico se rechaza en `occurredAt` antes de seleccionar, nunca se normaliza, y `getVigentCatalogPin` rechaza igual un `instant` no canónico. Consecuencias: una versión que entra en vigor entre la submission y la llamada real no es la fijada (la ventana es la latencia del walk), y un walk reanudado fija lo que fijó su primera corrida. Para P-15/D: elegir el pin con `getVigentCatalogPin(documentId, invocation.submittedAt)`, nunca con un reloj. Un segmento sin `model_version_id` resuelto nunca está cubierto (ADR 0092 Four): desde 2.9.0 no se despacha (Q-C1), y el prerequisito del smoke es que el catálogo cubra la versión exacta resuelta. Clase y moneda no se piden aquí (`PRICE_MISSING` es de la valoración). Cada rechazo nombra el campo y el instante y la versión vigente, nunca un precio, un cero ni una estimación. `Ledger.getVigentCatalogPin` y las dos funciones puras en el barrel son la única autoridad para elegir y para comprobar; `resolvePrice` sigue sin llamador | [ADR 0103](../../architecture/0103-a-delivery-pins-the-price-catalog-version-it-will-be-valued-against.md); [ejecución §7](../architecture/database/execution/index.md) |
| 124 | **Los constructores del efecto, la entrega y sus movimientos, cerrados por construcción** (P-15/C; ADR 0080 §7; Q-C2; C-D2): `buildEffectIntentionEvent` deriva `effectId`, su clave de idempotencia y el digest lógico con las funciones del ledger, nunca reescritas; `buildDispatchIntentionEvent` recibe el pin como entrada obligatoria, así que no puede emitir una entrega sin pin; `buildDispatchTransitionEvent` es discriminado, un literal por brazo: `INFLIGHT` (instante de aceptación y handle, sin terminal ni desenlace), `ABANDONED` (instante terminal, nunca aceptación ni handle) y `SETTLED` (instante terminal, desenlace y par de resultado sólo cuando lo hay). Los tres rechazan una invocación V1 por nombre, esparcen `payloadCoordinate` y quedan fuera del barrel del runtime hasta D (`RUNTIME_PUBLIC_EXPORTS` sigue en 285). Nombres de transición acotados: `effect-intended.` + 64 hex = 80; la intención y los movimientos de una entrega por el tipo más el sha-256 del id, así que ningún id desborda el límite de 120. Los fixtures anteriores a 2.9.0 que escriben una entrega por la puerta real tuvieron que ganar un pin, porque la versión vigente lo exige (ampliación del DT a tres rutas de fixtures) | [ADR 0103](../../architecture/0103-a-delivery-pins-the-price-catalog-version-it-will-be-valued-against.md); [ejecución §7](../architecture/database/execution/index.md) |
| 125 | **Una sola casa construye los tres registros de gasto, y L-P33B-1 admite a la puerta** (P-15/C; ronda 2 del DT): **L-P15C-1** (nueva, con scope de ruta; `PATH_SCOPED_LAWS` 150 → 151): en `src/` sólo `runtime/src/core/events/index.ts` escribe `type: "EFFECT_INTENDED"`, `"DISPATCH_INTENDED"` o `"DISPATCH_OUTCOME_RECORDED"` —o `type:` con cualquier constante de `src/` declarada como una de esas tres palabras—, y la casa debe construir los tres; límite declarado: un nombre renombrado al importar, un valor calculado en ejecución o el spread de un objeto que ya trae `type` no se ven. **L-P33B-1** se enmienda en su fila: `ledger/src/ledger/index.ts` es el único llamador admitido del concepto, exactamente por `selectVigentCatalogVersion` y `pinCovers`, con chequeo de admisión vencida, y no puede nombrar `resolvePrice`. Las gramáticas del ledger para efecto, entrega y desenlace no se cierran más en C | [ADR 0103](../../architecture/0103-a-delivery-pins-the-price-catalog-version-it-will-be-valued-against.md); [ejecución §7](../architecture/database/execution/index.md) |
| 126 | **Un empate de versiones se rechaza por ambiguo, y su prueba es pura** (P-15/C; Q-C3; C-R2): el registro no prohíbe hoy dos versiones de un documento con el mismo `effective_from` (su índice `(document_id, effective_from)` no es único y ninguna regla de puerta ordena los instantes), así que el selector es el único lugar que lo atrapa: devuelve `AMBIGUOUS` con las versiones empatadas y ninguna gana. La prueba que queda es pura (el empate va directo a `selectVigentCatalogVersion`); una prueba de puerta que siembra el empate por la puerta del registro existe sólo mientras C preceda a la regla que prohíba publicarlo, y desde entonces el rechazo del selector sólo es alcanzable en ledgers escritos antes | [ADR 0103](../../architecture/0103-a-delivery-pins-the-price-catalog-version-it-will-be-valued-against.md); [ejecución §7](../architecture/database/execution/index.md) |
| 127 | **Errata de P-14: el bootstrap del registry no se entregó ni se nombró como deuda, y lo paga P-15/R** (P-15/R; Fable C-D6): la tabla de packets (`:156`) pone en P-14 «el bootstrap del registry único y de la asignación de rol versionada», y ADR 0085 «Not in this record» difirió a P-14 B/C la puerta de producto que publica un `MODEL_VERSION` o una asignación; B y C entregaron sólo las puertas de iniciativa y de ingreso, y ningún `src/` llamaba a `appendRegistryEvent`. P-15/R lo entrega después de C y antes de D, para que el drill de D siembre por una puerta real. ADR 0085 no se edita: ADR 0104 lo nombra y esta fila lo registra | [ADR 0104](../../architecture/0104-registry-configuration-is-published-through-one-door-that-derives-its-digest.md); [packets](../implementation/packets/index.md) |
| 128 | **Una sola puerta publica el registro de un primer task: deriva, reproduce o rechaza, y es sólo CLI** (P-15/R; ND-R2): `publishRegistryDocument` (concepto del ledger con su hoja de tipos) publica `MODEL_VERSION`, `ROUTING_ASSIGNMENT_GLOBAL` y `PRICE_TABLE` y rechaza por nombre las otras once clases (`REGISTRY_KIND_NOT_PUBLISHABLE`). Deriva y nunca acepta el digest, la clave `registry/<documentId>/<documentVersion>`, un `eventId` UUID v5 sobre esa clave bajo `REGISTRY_PUBLICATION_UUID_NAMESPACE` (fijado por vectores calculados fuera del paquete), los instantes de la puerta y la versión de contrato. No valida por segunda vez: la forma y el linaje son de la puerta del ledger, cuyo rechazo se contesta `REGISTRY_DOCUMENT_REFUSED` con el campo y la palabra cerrada. Idempotente por documento y versión (`getRegistryDocumentVersion`, verbo nuevo): misma clase, digest, padre e instante es un replay sin append —el autor y los instantes de la puerta no se comparan—; cualquier otra diferencia es `REGISTRY_VERSION_CONFLICT` nombrando el campo, nunca una versión nueva; quien pierde la carrera relee y decide. La puerta es `acp registry`, sexto verbo que escribe, por la escalera de uid y el `openForWrite` único (L-B4B-11 intacta); `SURFACE_MAP` la registra `CLI_ONLY` y ninguna ruta la sirve, así que `API_CONTRACT_VERSION` no se mueve. Un `PRICE_TABLE` nunca recibe un precio, una moneda o un fin que el dueño no dio | [ADR 0104](../../architecture/0104-registry-configuration-is-published-through-one-door-that-derives-its-digest.md) |
| 129 | **Una versión de `PRICE_TABLE` por instante y por documento, como invariante de escritura** (P-15/R; Q-C3): la puerta del registro rechaza en `effectiveFrom`, con `REGISTRY_EFFECTIVE_FROM_TAKEN`, una versión de un `PRICE_TABLE` cuyo `effective_from` ya tiene otra versión del mismo documento. Sólo para esa clase —la única cuya vigencia define C3— y por documento. No es un índice `UNIQUE`: una migración abortaría sobre un ledger que ya tiene el empate y el rebuild no tiene puerta; el fold sigue tolerándolo (asimetría de la decisión 56) y la puerta de despacho de ADR 0103 rechaza el pin ambiguo de esa historia. `MIGRATIONS` no se mueve. Desde aquí es verdad la frase de la decisión 126: el rechazo `AMBIGUOUS` sólo es alcanzable en ledgers escritos antes; la prueba de puerta de C-R2 se convierte para afirmar el rechazo de la siembra, con delta cero | [ADR 0104](../../architecture/0104-registry-configuration-is-published-through-one-door-that-derives-its-digest.md); [ADR 0103](../../architecture/0103-a-delivery-pins-the-price-catalog-version-it-will-be-valued-against.md) |
| 130 | **El digest de un documento en línea es el de su payload, y la puerta lo verifica** (P-15/R; ND-R1 (a); C-R1): para `MODEL_VERSION`, `ROUTING_ASSIGNMENT_GLOBAL` y `PRICE_TABLE` (`INLINE_CONTENT_DOCUMENT_KINDS`), cuyo contenido viaja en el evento (decisiones 71 y 88) y no se publica como artefacto, `content_digest` es el SHA-256 del JSON canónico del payload; las demás clases conservan el digest del artefacto. Errata en la fila `content_digest` de [streams §4](../architecture/database/streams/index.md). La puerta del registro lo verifica primero en `#assertRegistryDocumentAdmissible` para todo escritor —no sólo la CLI— y rechaza en `contentDigest` con `REGISTRY_CONTENT_DIGEST_MISMATCH`; corre después del replay y del linaje, así que un replay exacto sigue siendo replay. El fold no re-verifica: la historia guarda sus digests (decisión 56). Una versión anterior a R con digest de relleno, republicada por la puerta, choca en `contentDigest`. Publicar el payload como artefacto (b) se rechaza. Las suites que publicaban esas clases con digests de relleno —incluidas las cinco copias de `plantFixtureCatalog` de C— los derivan | [ADR 0104](../../architecture/0104-registry-configuration-is-published-through-one-door-that-derives-its-digest.md); [streams §4](../architecture/database/streams/index.md) |
| 131 | **Un solo llamador de `appendRegistryEvent` en `src/`** (P-15/R; C-R3): **L-P15R-1** (nueva, con scope de ruta; `PATH_SCOPED_LAWS` 151 → 152), en el molde F1 de L-P07C-1: el árbol sintáctico de todo `src/` `.ts`/`.tsx` rastreado, sin comentarios; una llamada cuyo callee es un acceso a propiedad `appendRegistryEvent`, o un acceso por elemento con esa cadena o una plantilla sin sustitución, se admite sólo en `ledger/src/registry-publication/index.ts` (exactamente una: control positivo y guarda de obsolescencia) y en el ledger que define la puerta. Cero archivos leídos falla. Límite declarado, por familia: invocación por `Function.prototype`, `Reflect.apply`, un método desestructurado o con alias, y una clave calculada por identificador | [ADR 0104](../../architecture/0104-registry-configuration-is-published-through-one-door-that-derives-its-digest.md) |
| 132 | **La apertura reutiliza el `attempt` plano de su coordenada, y intake → apertura → descubrimiento son un solo intento** (P-15/D1; adjudicación v2 C2; enmienda ADR 0080 §4 y §5): la puerta del ledger ya no asigna `1 + MAX(attempt)` a una apertura cuya coordenada tiene eventos —el `TASK_DISCOVERED` del intake, escrito antes de cualquier apertura—: reutiliza su `attempt` plano, y dos `attempt` planos distintos en una coordenada rechazan la apertura sin elegir; el fold nunca recalculó la asignación, así que no hay espejo. El productor propone por la misma regla. La apertura de una tarea con intake es el mismo evento —transición, índice, clave, id y payload— desde `DISCOVERED` (`INTAKE_ATTEMPT_OPENING_STEP`), y `nextStep` la devuelve cuando la tarea está `DISCOVERED` sin apertura bajo su clave V2. La continuidad lee el primer evento como intake (por el contrato y `taskIntakePayloadOf`) y le exige cada hecho compartido —tarea, `attempt` plano, instante de la submission, iniciativa y los cinco campos de la revisión—; después reconstruye la apertura byte a byte. `restateInvocation` lee una tarea con intake: la apertura bajo su clave V2, con la revisión del intake campo por campo; se invierte N-B-8 de B | [ADR 0105](../../architecture/0105-the-daemon-consumes-a-recorded-task-and-appends-the-whole-chain.md); [ADR 0080](../../architecture/0080-the-producer-speaks-the-v2-coordinate.md); [ejecución §3, §4, §7](../architecture/database/execution/index.md) |
| 133 | **El lector de la tarea registrada** (P-15/D1; C2): `readRecordedTask({ledger, plane, taskId, route})`, concepto nuevo del runtime con su hoja de tipos, lee de vuelta una tarea que el intake registró, o rechaza con un vocabulario cerrado y ordenado (`ENVELOPE_DIGEST_MISMATCH`, `ENVELOPE_UNREADABLE`, `INTAKE_UNREADABLE`, `ROUTE_DISAGREES_WITH_INTAKE`, `TASK_UNKNOWN`). El primer evento es el intake de esta tarea; `revisionId`, `envelopeSha256` y la referencia del sobre se leen por nombre (ausente, nulo, vacío o de otro tipo se rechaza en su ruta) y el resto por `taskIntakePayloadOf`. El sobre se lee del plano privado por referencia bajo el alcance de la tarea, se parsea con `TaskEnvelope` y debe dar el digest del intake con el único codificador del ledger; el rechazo del plano viaja por nombre. La revisión es la del intake; `submittedAt` es el `occurredAt` del intake, exigido en la forma canónica con el `isInstant` del ledger (offset o sin milisegundos: `INTAKE_UNREADABLE` en `intake.occurredAt`), porque aguas abajo el pin del catálogo y la puerta de despacho lo comparan como texto; el digest de la submission es `canonicalSubmissionDigest` sobre la ruta elegida, que debe coincidir con la resolución del intake en proveedor, alias y transporte. Nada se acuña, nada se escribe; la ruta es parámetro en D1 y D3 pasa la de su config | [ADR 0105](../../architecture/0105-the-daemon-consumes-a-recorded-task-and-appends-the-whole-chain.md) |
| 134 | **La recuperación contrasta la revisión con su read model** (P-15/D1; nota N1 de la verificación de B): `restateInvocation` aceptaba cualquier texto no vacío en `revisionId`, `envelopeSha256` y la referencia del sobre de la apertura; ahora los contrasta, campo por campo, con `Ledger.getTaskRevision(taskId, revisionNumber)` (verbo nuevo, y miembro nuevo de `LifecycleRecoveryPort`). Fila ausente: `DISCOVERY_UNREADABLE` en `attempt.revision`; diferencia: `DISCOVERY_UNREADABLE` en `attempt.revision.<campo>`. No `SUBMISSION_DIGEST_MISMATCH`, que proponía el brief (esta fila corrige la palabra del brief, y el DT la confirmó): la preimagen del digest de la submission es tarea, intento, instante, iniciativa y ruta, sin ningún campo de revisión, así que una revisión discordante es una apertura que esta puerta no puede atribuir —la palabra que la decisión 119, corregida, dio al invocationId— | [ADR 0105](../../architecture/0105-the-daemon-consumes-a-recorded-task-and-appends-the-whole-chain.md) |
| 135 | **Endurecimiento del ledger: transporte del segmento, instantes canónicos de la transición, un solo `isInstant`** (P-15/D1; Fable de C (i) y residuales v2): una sola lectura, `segmentTransportRefusal` en la proyección, rechaza en `payload.segment.transportKind` un transporte ausente, nulo, vacío, de otro tipo o ajeno a `TRANSPORT_KINDS`: la puerta la lanza y `applyEventToSnapshot` lanza el mismo issue, así que el rebuild de una historia que tenga uno rechaza por nombre con las palabras de la puerta en vez de morir después en una clave foránea (decisión 56; C1 de la postauditoría de Fable). `acceptedAt` y `terminalAt`, que P-18 ordena como texto, se exigen en la forma canónica donde cualquiera se compare: `dispatchOutcomeRecord`, que leen la puerta y el fold, rechaza en su clave un valor presente en otra grafía (offset, sin milisegundos, `z` minúscula, fecha que no vuelve igual, vacío) y nunca lo normaliza. `isInstant` vive una vez en el paquete del ledger, en `ledger/src/projection`, con su gramática privada, exportado para la puerta y en el barrel para el lector de la tarea registrada; se pliegan en él todos los demás predicados de instante del paquete: la copia de la puerta, las de outbox y versión de modelo de la proyección y —tras el C1 del verificador— el `occurredAt` de la observación de uso y el instante de activación de la integridad de cuentas, que sólo miraban la forma y admitían una fecha inexistente como el 30 de febrero; un grep de `ledger/src` no encuentra otro. La afirmación se limita al paquete del ledger: las copias de `contracts/src/schemas/artifact-record` (`:142`), `runtime/src/enforcement` (`:205`), `protocol/src/schemas` (`:2182`, el `CanonicalInstant` de P-15/R) y `daemon/src/arbiter` (`:78`) quedan para P-37, y la casa única final debe ser `@acp/contracts`, porque contracts y protocol no pueden importar el ledger. Un `effective_from` de registro no canónico ya lo rechazaba `verifyIntegrity` por la forma del documento y la igualdad de columnas; D1 lo fija con una fila plantada. Fuera de D1: el `requested_at` de entregas anteriores a 2.9.0 y `resolvePrice` confiando en el instante del llamador (nota para P-33) | [ADR 0105](../../architecture/0105-the-daemon-consumes-a-recorded-task-and-appends-the-whole-chain.md); [ejecución §3, §4, §7](../architecture/database/execution/index.md) |

---

## 3. Diseño explícitamente superseded

Se lista para que nadie lo reintroduzca creyendo que sigue vigente.

| Idea | Motivo del retiro |
| --- | --- |
| Factories obligatorias de cero argumentos | favorecen globals ocultos; inyección explícita en el composition root |
| Mudanza atómica masiva del árbol como prerequisito | bloquea entregar función útil; la anatomía se aplica por funcionalidad |
| «Un caso de uso, un archivo» como ley | produce archivos gigantes o abstracciones prematuras |
| Artefactos sin límite y vault sin borrado | contradice almacenamiento acotado; retención por clase y tombstone explícito |
| «Nada sale de la máquina» | falso en un producto que usa modelos y telemetría externos; egress explícito y mínimo |
| El journal del motor durable es una cache descartable | retirar su estado sin exportar timers, señales y efectos pierde continuidad |
| Enum cerrado de proveedores como mecanismo de extensibilidad | totalizar un `Record` es higiene; la extensibilidad es un descriptor validado |
| Estado real de una cuenta y prorrateo contable como una sola medida | son dos cosas; una decide admisión, la otra factura |
| Congelar la expansión del motor externo equivale a resolver la recuperación | son problemas separados: uno es alcance, el otro es corrección del journal |
| Roles de staffing dinámico de producto = staffing de agentes de este repositorio | son dos vocabularios distintos y no se mezclan |
| Ratio carpetas/archivos, lista negra universal y `grep` de marca como criterios de calidad | miden forma, no responsabilidad ([calidad §2](../quality/index.md)) |
| Estados de cuenta `READY` y `DRAINED` como nombres nuevos | el enum real es `AVAILABLE`, `DRAINING`, `EXHAUSTED`, `COOLDOWN`, `AUTH_REQUIRED`; se preserva y se mapea, no se inventan alias |
| «Kimi K3 dirige, audita y gestiona commits; Opus implementa. Codex revisa ediciones propias de Kimi e hitos importantes» ([coordinación](../kickoff.md) título, §1–§3, la titularidad de commits de §5 y el prompt de §6) | superseded por orden del owner del 2026-09-22 ([§1](#1-mandatos-del-owner-vigentes), [coordinación §0](../kickoff.md)): Kimi deja de ser DT; las ediciones propias del DT van a un verificador independiente y no a Codex; los hitos reciben auditoría read-only de Fable a criterio del DT; Codex pasa a consultor ocasional vía owner. Los briefs, adjudicaciones y receipts emitidos bajo Kimi quedan como registro histórico |

---

## 4. Elecciones abiertas, sin bloquear el trabajo

Cada una nombra **cuándo** deja de poder postergarse. Ninguna impide abrir el
primer packet.

| Elección | Recomendación inicial | Cuándo hay que resolverla |
| --- | --- | --- |
| Cambiar de proveedor en un run activo | tarea o iniciativa nueva, y reanudar desde un checkpoint compatible; un run activo conserva su driver hasta una frontera segura | antes de prometer migración transparente de runs activos. Se consultó al owner; sin respuesta no se presume migración en caliente |
| Segundo motor externo | mantener el actual; evaluar un segundo si aporta un perfil concreto. El supervisor local debe cumplir lo que anuncia | antes del packet que agregue un motor nuevo; no hace falta instalarlo para cerrar los hallazgos actuales |
| Perfil de proveedores del primer release | un CLI de suscripción operativo más una ruta de API y una local reales; el resto explícitamente no soportado hasta su conformidad | antes del smoke real y de la matriz de integraciones; exige elegir cuenta y límite de consumo |
| Segundo backend de datos | definir puerto y migraciones desde ahora; elegir el motor adicional por necesidad real | antes de anunciar intercambio de base a usuarios |
| Login local, escritorio y comercial | operador local primero; identidad federada sólo detrás de un adapter | en el diseño de distribución; no bloquea el backend local |
| Audio, voz, imagen, retrieval y comunicación entre agentes | contratos extensibles y perfiles separados; priorizar un caso real | antes del pack respectivo; audio en tiempo real necesita diseño propio |
| Adapters de terceros instalables | packs auditables y versionados con permisos explícitos; no ejecutar plugins arbitrarios dentro del dominio | antes de distribuir plugins |
| Recibos históricos ausentes | revalidar retrospectivamente o exceptuar con una lista nombrada | antes de la certificación; es decisión del owner, no del writer |
| Conflictos históricos de versión de cuenta, si el preflight los encuentra | resolver caso por caso y registrar aquí; **nunca** deduplicar en silencio | durante la migración de integridad de cuentas |
| Verificación de los objetivos numéricos de release | los números **ya están fijados** como objetivos de diseño en [tests §9](../quality/testing/index.md), con su perfil y su carga. Lo que falta es medirlos y aceptarlos | antes de la certificación. **Un objetivo no se redefine después de una corrida**: cambiarlo es una decisión previa, versionada y con motivo |

**No se pide al owner que resuelva cada detalle técnico.** Se le pide decisión
cuando cambia el alcance del producto, el gasto, las credenciales, la publicación o
el acceso a repositorios fuera de este.

---

## 5. Relación entre el programa anterior y esta especificación

El programa anterior alcanzó un registro acotado con siete criterios y cinco
deudas declaradas; su checker tiene contraejemplos que corregir. Esta fase **no**
vuelve a marcar todo en cero: reutiliza lo acreditado y mantiene las deudas
explícitas.

Tres clases de trabajo, deliberadamente separadas:

1. **Correcciones de garantías existentes:** identidad, contenido perdido, éxito
   falso, replay, evidencia, privacidad y tests inseguros.
2. **Ensamblaje pendiente:** clientes compuestos, checkpoints leídos, cuentas,
   daemon residente, exporter conectado y flujo completo.
3. **Funcionalidades ampliadas:** planificación y economía, registry de
   integraciones y packs de proveedores y frameworks.

Esta separación evita la confusión anterior entre terminar un programa de leyes,
certificar un producto y agregar casos de uso nuevos.
