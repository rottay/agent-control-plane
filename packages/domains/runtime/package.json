{
  "name": "@acp/runtime",
  "version": "0.0.0",
  "type": "module",
  "description": "Durability domain for the Agent Control Plane. One shared lifecycle engine, the orchestration port, and the SQLite supervisor over the append-only event log. The Restate edge lives in @acp/durability.",
  "license": "MIT",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build"
  },
  "dependencies": {
    "@acp/accounts": "workspace:*",
    "@acp/contracts": "workspace:*",
    "@acp/ledger": "workspace:*"
  },
  "devDependencies": {
    "vitest": "3.2.4"
  }
}
