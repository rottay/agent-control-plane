{
  "name": "@acp/providers",
  "version": "0.0.0",
  "type": "module",
  "description": "Read-only provider adapters for the Agent Control Plane. One process boundary, normalized events, no ledger reach and no product writes.",
  "license": "MIT",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build"
  },
  "dependencies": {
    "@acp/contracts": "workspace:*"
  },
  "devDependencies": {
    "vitest": "3.2.4"
  }
}
