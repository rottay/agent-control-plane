{
  "name": "@acp/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Observation CLI for the Agent Control Plane. Every read verb opens the ledger query-only; four named verbs share one short-lived writable handle: tool-call appends one receipt, cancel appends one cancellation, attach appends nothing, and initiative appends one registration.",
  "license": "UNLICENSED",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "acp": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build"
  },
  "dependencies": {
    "@acp/accounts": "workspace:*",
    "@acp/durability": "workspace:*",
    "@acp/protocol": "workspace:*",
    "@acp/ledger": "workspace:*",
    "@acp/observation": "workspace:*",
    "@acp/runtime": "workspace:*",
    "@acp/tools": "workspace:*"
  },
  "devDependencies": {
    "vitest": "3.2.4"
  }
}
