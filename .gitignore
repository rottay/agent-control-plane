# --- Dependencies -----------------------------------------------------------
node_modules/
.pnpm-store/
.pnpm-debug.log*

# --- Build output -----------------------------------------------------------
dist/
build/
out/
*.tsbuildinfo
.tsbuild/

# --- Test / coverage --------------------------------------------------------
coverage/
.vitest/
*.lcov

# --- Local databases (ledger + read models are local-only artifacts) --------
*.sqlite
*.sqlite3
*.db
*.db-wal
*.db-shm
*.sqlite-wal
*.sqlite-shm
data/
.local-db/

# --- Restate local runtime data --------------------------------------------
.restate/
restate-data/
restate-server-data/

# --- Local-only working root (P2) ------------------------------------------
# Pinned external tools, disposable toy repositories and drill databases, pid
# and log files. Everything the durability plane writes lives under here, and
# none of it is evidence: it is reproducible from the ledger or discardable.
.acp-local/

# --- Logs -------------------------------------------------------------------
logs/
*.log
npm-debug.log*
yarn-error.log*

# --- Provider config roots (isolated per-account CLI homes) -----------------
.provider-roots/
.config-roots/
.claude-home/
.codex-home/
.kimi-home/

# --- Credential stores: never committed, in any location --------------------
# Narrow on purpose: the roadmap forbids secrets in the repo, but P0 evidence
# (receipts, audits, fixtures) must stay trackable. Do not broaden to *.json.
accounts.local.json
**/accounts.local.json
.env
.env.*
!.env.example
*.pem
*.key
*.p12

# --- OS / editor noise ------------------------------------------------------
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
